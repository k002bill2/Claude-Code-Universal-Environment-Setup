---
tags:
  - claude
  - setup
---

# Claude Code 실전 예제
#claude-code #examples #practical-guide #2026-03-update

> 최종 업데이트: 2026-07-15
> Claude Code: v2.1.210 | Opus 4.8 / Fable 5 / Sonnet 5 (1M 컨텍스트) / Haiku 4.5
> 설정: .claude/settings.json

## 🎯 실제 워크플로우 예제

### 예제 1: 새 React 프로젝트 시작하기

#### Step 1: 프로젝트 초기화 (macOS)
```bash
# 프로젝트 생성 (Next.js 15 최신)
npx create-next-app@latest my-app --typescript --tailwind --app
cd my-app

# Claude Code 설정
mkdir -p .claude/skills .claude/commands .claude/agents .claude/plugins
```

#### Step 2: CLAUDE.md 작성
```markdown
# My App - E-commerce Platform

## Project Overview
- **Purpose**: Online shopping platform
- **Tech Stack**: Next.js 15, TypeScript, Tailwind CSS, Drizzle ORM
- **Features**: Product catalog, Cart, Checkout, User auth

## Current Tasks
1. Set up authentication with Supabase Auth
2. Create product listing page
3. Implement shopping cart
4. Add payment integration

## Code Standards
- Use Server Components by default
- Client Components only when needed
- Proper error boundaries
- Loading states for all async operations
```

#### Step 3: Claude Code 세션 시작
```bash
claude

# effort 설정
/effort high

# 첫 작업 요청
"Using the project context in CLAUDE.md, set up the basic authentication system with Supabase Auth and create the initial user model"

# Sub-agent 생성
/agents
# Create 'auth-specialist' agent for authentication tasks

# Skill 생성
"Use the skill-creator to create a skill for generating Drizzle ORM models"
```

---

### 예제 2: 버그 수정 워크플로우

#### Scenario: Production 버그 발생
```bash
# Claude Code 시작
claude

# 버그 정보 제공
"We have a critical bug in production. Users report that the shopping cart total is calculating incorrectly when applying discount codes. The issue seems to be in the calculateTotal function in src/utils/cart.ts"

# Explore 에이전트로 빠른 분석
"Use the Explore agent to analyze the calculateTotal function and related code"

# 테스트 작성
"Create comprehensive unit tests for the calculateTotal function covering all edge cases with discount codes"

# 수정 및 검증
"Fix the bug and verify all tests pass"

# 코드 리뷰
"Review the changes using the code-reviewer skill"
```

---

### 예제 3: 새 기능 개발 (전체 플로우)

#### Feature: 사용자 대시보드 추가

##### 1. Planning Phase
```bash
# Plan 에이전트 활용
"Use the Plan agent to design the implementation for a user dashboard with:
- Order history
- Favorite products
- Account settings
- Recent activities"
```

##### 2. Dev Docs 생성
```bash
# 계획 승인 후
/dev-docs
# 또는
"Create dev docs for this approved plan"
```

##### 3. Implementation (병렬 에이전트 활용)
```bash
# Worktree 격리로 백엔드/프론트엔드 병렬 작업
"Implement the dashboard API endpoints using a worktree-isolated agent"

"In parallel, create the dashboard layout component"
```

##### 4. Testing
```bash
# test-automation-specialist로 검증 (v2.1.78의 verify-agent 대체)
"Use the test-automation-specialist to run build, test, and lint verification"
```

##### 5. 메모리 저장
```bash
# 중요 결정사항 기억
"Remember that we use Redis for dashboard activity caching with 5-minute TTL"
```

---

### 예제 4: 코드 리팩토링 프로젝트

#### Large-scale 리팩토링 with Parallel Agents

```bash
# 메인 orchestrator 설정
claude
/effort high

"I need to refactor our legacy JavaScript codebase to TypeScript.
The project has about 50 files. Create a systematic approach using multiple agents."

# Claude가 자동으로 여러 agents 활용:
# - Plan agent: 마이그레이션 계획 수립
# - architect: 타입 시스템 설계
# - general-purpose agents: 파일별 변환 (Worktree 격리)
# - test-automation-specialist: 타입 체크 및 검증
```

---

### 예제 5: 플러그인 개발 워크플로우 (신규)

#### Custom 플러그인 생성

```bash
claude

# 플러그인 구조 생성
"Create a new Claude Code plugin called 'my-lint-toolkit' that:
- Includes a skill for TypeScript best practices
- Has an agent for automated code review
- Adds PostToolUse hooks for auto-formatting"

# 생성된 구조
.claude/plugins/my-lint-toolkit/
├── plugin.json
├── skills/
│   └── ts-best-practices/SKILL.md
├── agents/
│   └── auto-reviewer.md
└── hooks/
    └── auto-format.sh
```

---

### 예제 6: 메모리 시스템 활용 워크플로우 (신규)

#### 세션 간 맥락 유지

```bash
# 세션 1: 아키텍처 결정
claude
"우리 프로젝트에서 인증은 Supabase Auth를 사용하고,
RLS를 모든 테이블에 적용하기로 했어. 이걸 기억해줘."
# → 자동으로 project memory 저장

# 세션 2: 새 기능 추가
claude
"Create a new 'orders' table with proper RLS policies"
# → Claude가 메모리에서 Supabase Auth + RLS 결정을 자동 참조

# 메모리 확인
/memory
```

---

### 예제 7: Effort + Fast 모드 활용 (신규)

```bash
# 복잡한 설계 작업 - 높은 추론 강도
/effort high
"Design the microservices architecture for our e-commerce platform"

# 간단한 수정 - 빠른 모드
/fast
"Fix the typo in the README"

# 일반 개발 - 기본값
/effort medium
"Implement the user profile page"
```

---

### 예제 8: Worktree 기반 병렬 작업 (신규)

```bash
claude

# 두 개의 독립 작업을 Worktree로 병렬 실행
"I need two changes done in parallel:
1. Refactor the auth module (use worktree isolation)
2. Add new API endpoints for dashboard (use worktree isolation)

Both should run their own tests before merging."

# Claude가 자동으로:
# - Agent 1: worktree에서 auth 리팩토링 + 테스트
# - Agent 2: worktree에서 API 추가 + 테스트
# - 결과 merge 또는 branch 반환
```

---

### 예제 9: CI/CD 파이프라인 구성

#### GitHub Actions 워크플로우 생성

```bash
# DevOps skill 활용
"Using the devops-automation skill, create a complete CI/CD pipeline for our Next.js application with:
- Automated testing on PR
- Type checking
- Linting and formatting
- Build verification
- Deployment to Vercel on main branch
- Slack notifications"

# 생성된 파일
.github/workflows/
├── ci.yml          # PR 체크
├── cd.yml          # 배포
└── notify.yml     # 알림
```

---

### 예제 10: Workflow 도구로 다단계 자동화 (신규)

#### Workflow 스크립트로 "계획 → 병렬 구현 → 항목별 파이프라인" 오케스트레이션

> Workflow 도구는 **인라인 JS 스크립트**다. 반드시 순수 리터럴인
> `export const meta = { name, description, phases }` 로 시작하고,
> 본문에서 전역 함수(`agent`, `parallel`, `pipeline`, `phase`, `log`, `args`)를
> `await` 로 직접 사용한다. (※ 2026-06-16 갱신: `workflow.pipeline([...])`,
> `.withIsolation().run()`, `workflow.background()` 같은 fluent 체인은 실재하지 않는다.)

```js
// 사용자 대시보드 기능을 계획 → 병렬 구현 → 검증까지 자동화하는 Workflow
export const meta = {
  name: "dashboard-feature",
  description: "대시보드 기능을 계획하고 백엔드/프론트를 병렬 구현 후 검증",
  phases: ["plan", "build", "verify"],
};

// 1) 계획 단계 — schema를 주면 검증된 객체를 그대로 반환받는다
phase("plan");
const plan = await agent(
  "사용자 대시보드(주문 내역/즐겨찾기/계정 설정)의 구현 계획을 세워라. " +
    "백엔드 API와 프론트엔드 컴포넌트 작업을 분리해 작업 목록으로 나눠라.",
  {
    label: "planner",
    phase: "plan",
    agentType: "Plan",
    effort: "high",
    schema: {
      type: "object",
      properties: {
        backendTasks: { type: "array", items: { type: "string" } },
        frontendTasks: { type: "array", items: { type: "string" } },
      },
      required: ["backendTasks", "frontendTasks"],
    },
  },
);
log(`백엔드 ${plan.backendTasks.length}건 / 프론트 ${plan.frontendTasks.length}건`);

// 2) 구현 단계 — parallel은 "함수(thunk) 배열"을 받아 동시 실행 + 배리어(모두 완료 대기)
//    각 에이전트는 자체 git worktree에서 격리 실행된다 (미변경 시 자동 정리)
phase("build");
const [backend, frontend] = await parallel([
  () =>
    agent(`다음 백엔드 작업을 구현하라:\n${plan.backendTasks.join("\n")}`, {
      label: "backend",
      phase: "build",
      isolation: "worktree",
      agentType: "general-purpose",
    }),
  () =>
    agent(`다음 프론트엔드 작업을 구현하라:\n${plan.frontendTasks.join("\n")}`, {
      label: "frontend",
      phase: "build",
      isolation: "worktree",
      agentType: "general-purpose",
    }),
]);
// 실패한 항목은 null로 들어온다
if (!backend || !frontend) log("일부 구현 실패 — 검증 단계에서 확인 필요");

// 3) 검증 단계 — pipeline은 항목별 독립 파이프라인(단계 간 배리어 없음)
//    각 모듈을 "리뷰 → 테스트"로 흘려보낸다
phase("verify");
await pipeline(
  ["backend", "frontend"],
  (mod) =>
    agent(`${mod} 모듈을 code-reviewer 관점으로 검토하라`, {
      phase: "verify",
      agentType: "code-reviewer",
    }),
  (mod) =>
    agent(`${mod} 모듈의 build/test/lint를 실행하고 결과를 보고하라`, {
      phase: "verify",
    }),
);
```

> Workflow는 동시 실행을 자동으로 `min(16, CPU 코어-2)`로 캡하고,
> 총 1000 에이전트 상한을 둔다. 백그라운드로 실행되며 완료 시 알림이 온다.

---

## 🛠️ Skills 활용 실전 예제

### Custom Skill 만들기: PR Description Generator

```markdown
# .claude/skills/pr-generator/SKILL.md
---
name: pr-generator
description: Generate comprehensive PR descriptions from git diff and commit messages
---

# PR Description Generator

## Instructions
1. Analyze git diff
2. Read commit messages
3. Identify changed files
4. Detect breaking changes
5. Generate PR description

## Output Format
\`\`\`markdown
## Summary
[Brief description of changes]

## Changes
- Feature: [New features added]
- Fix: [Bugs fixed]
- Refactor: [Code improvements]

## Breaking Changes
[Any breaking changes]

## Testing
- [ ] Unit tests updated
- [ ] E2E tests passed
- [ ] Manual testing completed

## Screenshots
[If UI changes]

## Related Issues
Closes #[issue-number]
\`\`\`
```

---

## 🔄 일일 워크플로우 예제

### 아침 루틴
```bash
# 1. 세션 시작
claude

# 2. 메모리 확인
/memory

# 3. 일일 상태 체크
"Check the status of our project:
- Any failing tests?
- Pending TODOs in code?
- Open pull requests?
- Performance metrics from yesterday?"

# 4. 우선순위 설정
"Based on the current status, what should be today's priorities?"
```

### 코드 리뷰 세션
```bash
# PR 리뷰 자동화
"Review all changes in PR #123 using the code-reviewer skill.
Focus on:
- Security issues
- Performance implications
- Code style consistency
- Test coverage"
```

### 일일 마무리
```bash
# 일일 보고서 생성
"Generate a daily progress report:
- Completed tasks
- Commits made
- Issues resolved
- Tomorrow's priorities"

# 메모리에 중요 사항 저장
"Remember that we decided to defer the payment integration to next sprint"
```

---

## 💡 고급 팁과 트릭

### 1. Multi-Agent 협업
```bash
# 복잡한 기능을 여러 agent가 협업
"I need to implement a real-time notification system.
Coordinate between:
- Plan agent for system design
- architect for architecture
- general-purpose for implementation
- test-automation-specialist for testing"
```

### 2. Context Preservation
```bash
# 긴 작업을 여러 세션으로 분할
/compact  # 컨텍스트 압축
"Save progress to dev docs"

# 또는 메모리로 핵심 정보 보존
"Remember: We're at Phase 2, Step 3 of the dashboard implementation"
```

### 3. Automated Workflows
```json
// .claude/settings.json - 자동 워크플로우
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write(*.tsx)",
        "hooks": [
          {"type": "command", "command": "npx tsc --noEmit"},
          {"type": "command", "command": "npm run lint:fix"}
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [{
          "type": "command",
          "command": "echo 'Context will be compacted - check dev docs'"
        }]
      }
    ]
  }
}
```

### 4. Performance Optimization
```bash
# 큰 파일 처리 시
"Process src/data/large-file.json in chunks of 1000 records"

# 간단한 작업은 빠르게
/fast
"Update all import paths from @old to @new"
```

---

## 🎓 학습 경로별 예제

### Beginner: 첫 컴포넌트 만들기
```bash
"Create a simple Button component with:
- TypeScript props
- Click handler
- Disabled state
- Loading state
- Basic styling with Tailwind"
```

### Intermediate: API 통합
```bash
"Integrate with the Stripe API:
- Set up payment endpoints
- Handle webhooks
- Implement error handling
- Add proper typing"
```

### Advanced: 마이크로서비스 구조
```bash
/effort high
"Design and implement a microservices architecture:
- API Gateway
- Service discovery
- Message queue integration
- Distributed tracing"
```

---

## 🚨 일반적인 문제 해결

### Case 1: Skill이 동작하지 않을 때
```bash
# 디버깅 단계
1. "List all available skills"
2. SKILL.md의 description 필드 구체화
3. YAML frontmatter 형식 확인
4. /compact 후 재시도
```

### Case 2: Agent가 잘못된 작업 수행
```bash
# Agent 수정
/agents
# Edit agent description to be more specific
# Limit tool access with disallowedTools
# Set maxTurns to prevent runaway execution
# Add constraints in system prompt
```

### Case 3: 컨텍스트 오버플로우
```bash
# 해결 방법
1. /compact로 컨텍스트 압축
2. 작업을 작은 단위로 분할
3. 불필요한 파일 컨텍스트 제거
4. Dev Docs에 진행 상황 저장 후 /clear
```

### Case 4: 메모리 관련 문제
```bash
# 메모리 확인
/memory

# 메모리가 로드되지 않으면
1. MEMORY.md 인덱스 파일 확인
2. 메모리 파일 frontmatter 형식 확인
3. description 필드가 구체적인지 확인
```

---

*실전 예제는 계속 업데이트됩니다. 마지막 업데이트: 2026-07-15*
*환경: macOS (VS Code, Cursor 호환) | Claude Code v2.1.210 | Opus 4.8 / Fable 5 / Sonnet 5 / Haiku 4.5*

#practical-examples #workflows #claude-code