# HANDOFF · 컨텍스트 예산 트리거 + 리서치 격리 구현

작성: 2026-07-27 · 작성 사유: 컨텍스트 예산 경고(WARNING) 발동 — 본 기능이 실전에서 스스로 트리거

> `.planning/STATE.md` 가 존재하나 그것은 GSD 워크플로우 상태이고 **이 작업을 추적하지 않는다.**
> 따라서 `/gsd:pause-work` 대신 이 문서를 쓴다 (규칙의 의도 = 이미 추적되는 것을 중복 저장하지 않기).

## 상태: 번들 전용 훅 전환 · **Q·R·S·T·U·V·W·X 전부 반영 · Codex 17라운드 대기**
> Q·S 조언자 재현 통과 → R 의 좁힘이 U 를 열어 **U·T 수정** → 파생 잔여 2건(토큰 추출·dedupe) 닫음
> → Codex 15라운드가 **V(심링크 선점 · 보안)** 지적 → **V 수정**
> → Codex 16라운드가 **W(임시디렉토리 해석 불일치)·X(cleanup 복원원 오류)** 지적 → **W·X 수정**(2026-07-30).
> Codex 14라운드의 Q/R/S 무지적은 통과 증명이 아니었다("못 찾음") — **U 가 그 증거다.**
> 남은 절차: **Codex 17라운드 → 커밋(사용자 지시 대기).**
>
> **중단 기준 (정정판 · 2026-07-31):** 다음 라운드가 **직전 수정이 도입·변경한 코드**에서
> 희귀 환경·변조 전용 P2 를 또 내면 **알려진 잔여에 기록하고 인계**한다(그 다음 라운드는 돌리지 않는다).
> 근거: 이 스레드는 봉합이 다음 변형을 낳는 패턴을 반복했고(Q→X), 그 연쇄를 끊는 선이 필요하다.
>
> **적용되지 않는 것 — 중요:** ① **직전 수정이 건드리지 않은 기존 결함** ② **이미 식별된 계열의
> 추가 인스턴스**(예: python↔jq 계약 불일치 = T·U·Y) ③ P0/P1 ④ 정상 경로 결함.
> 초판 문구("희귀 환경·변조 전용 경로")는 **너무 넓어 Y 수정을 막을 뻔했다** — 기준의 목적은
> *자기 수정이 만든 잔여*를 쫓는 것을 멈추는 것이지, **식별된 계열을 반쯤 닫은 채 출하하는 것이 아니다.**

> **먼저 읽을 곳: 문서 맨 아래 `## 다음 세션 인계 (2026-07-30 · 3차)`.**
> 중간의 `## 다음 단계`(구 라운드 기준)와 `## Codex 5~8라운드` 절들은 **이력**이며 최신이 아니다.
>
> 검증 시 픽스처 함정 4종(이 세션에서 실제로 3번 헛발질했다):
> ① 이미 설치된 파일을 픽스처에 복사하면 "설치 전 상태"가 아니다
> ② `settings.json` 의 `statusLine.command` 가 가리키는 파일명과 픽스처 파일명을 일치시켜라
> ③ 쓰기 실패는 `chmod 555`(디렉토리)가 아니라 `chflags uchg`(대상 파일)로 표적 차단하라 —
>    디렉토리를 막으면 설치기가 `mkdir agents` 에서 먼저 죽어 검증 대상에 도달조차 못 한다
> ④ **실패 케이스만 보지 말고 대조군(정상 설치)을 함께 돌려라** — 대조군 없이는
>    "차단 성공" 과 "애초에 도달 못 함" 을 구분할 수 없다
>
> **계열 요약:** J→N→O 는 같은 결함의 세 변형이었다 — "부분 실패를 관대히 넘기는 함수가 성공을
> 반환하고, 호출부가 그걸 믿고 되돌릴 수 없는 다음 단계를 진행". skip/실패/성공 3상태를 구분해
> 반환하지 않는 함수가 남아 있으면 다음 변형이 나온다. 인스턴스가 아니라 계열을 닫아라.
> 13라운드에서 **R(탐색 판정이 느슨) · S(스키마 불일치를 치환으로 삼킴) · Q(2회 백업이 원본을 덮어씀)**
> 로 세 갈래가 더 나왔다. 남은 점검축 셋: **① 판정이 부분일치인가 ② 예상과 다른 입력을 '고쳐서'
> 진행하는가 ③ 같은 실행에서 같은 자원을 두 번 건드리는가.**

## 산출물

브랜치 `feat/ctx-budget-trigger` · 초기 구현은 **커밋됨**(`f41ae81`):

```
docs/codex-advisor-worker-bundle/install.sh                      ← 브리지 삽입 함수 + 에이전트 heredoc
docs/codex-advisor-worker-bundle/README_조언자-작업자-전략.md
docs/codex-advisor-worker-bundle/REVIEW_컨텍스트예산-리서치격리.md   ← 설계 근거 정본
docs/codex-advisor-worker-bundle/HANDOFF.md                       ← 이 문서
```

5라운드 지적 반영분(install.sh + 위 문서 2종)은 **미커밋** — Codex 재검증 후 처리.

실환경(`~/.claude/`)에는 이미 설치 반영됨: `agents/researcher.md` 신설,
`agents/architect.md` tools 축소, `awesome-statusline.sh` 에 마커 블록 삽입,
`CLAUDE.md` 번들 블록에 3개 절 추가. **`settings.json` 은 무변경**(mtime `Jul 26 23:19` 유지).

## 설계 요약 (상세는 REVIEW 문서)

- 임계치 = **베이스라인 델타** (창 사용률 퍼센트가 아님 — 실측 baseline 50,905 = 200k 창의 25.5%라 창 퍼센트 임계는 양쪽 끝에서 깨짐).
  - **[현행 · 2026-08-08]** 델타 기준은 유지하되 임계 크기를 **창에 비례**시키고 비율을 **창 티어로 분리**했다:
    소형 창(< 500k) `window*15%` WARNING / `window*20%` CRITICAL, 대형 창(>= 500k) `window*40%` / `window*55%`.
    대형 완화 근거: Opus 5 는 1M 전 구간에서 지시 준수·도구 호출을 유지하므로 15/20% 를 그대로 쓰면
    델타 200k 에서 정지해 남은 800k 를 못 쓴다. 반대로 40/55% 를 200k 창에 적용하면 베이스라인 포함
    창의 80% 까지 진행돼 `Context 75% Rule` 을 넘는다 → 티어 분리.
  - **[구 — 이력]** 고정 상수 `51133`/`59000`. 사용자 지정 "200k 창 사용률 50~55%" 에서 역산한 값
    (`200,000 × 0.55 − 50,905`)이라 **200k 창 전용**이었고, 1M 창에서는 창의 6% 지점에 울려
    정상 작업 하나가 경고를 여러 번 띄웠다(규칙 사문화 위험) → 창 비례로 대체.
- **[현행]** 배선 = `awesome-statusline.sh` 마커 블록 → `$TMPDIR/claude-ctx-advisor-{sid}.json`(사실만 기록:
  `baseline/current/delta/window/window_pct`) → **번들 소유 훅** `~/.claude/hooks/advisor-context-budget.js`
  (settings.json `hooks.PostToolUse` 에 등록). 임계는 훅이 `delta` 와 `window` 로 직접 판정한다
  (창 티어별 비율 — 위 "임계치" 항목 참조. `window` 부재/이상 시 200k 폴백 → 소형 티어).
  디바운스 5회 + 승격 시 즉시 발화는 훅이 자체 구현(구 GSD 훅에서 공짜로 받던 것)
- **[폐기됨 — 이력]** 구 배선은 `claude-ctx-{sid}.json` 에 `remaining_percentage = 100 - delta*100/78666` 를
  **합성**해 GSD 플러그인 훅에 태웠다. 그 기교(`78666 = floor(59000/0.75)`, 정수 나눗셈 경계 보정)는
  **전량 제거됐다.** 지금 코드에서 `remaining_percentage`·`78666` 을 찾으면 안 된다.
  폐기 이유: ① GSD 플러그인 버전 의존(R1, High) ② 주입 문구의 `Usage at X%` 가 창 사용률이 아니라
  예산 소진율이라 상태줄과 모순돼 보임(R3). 둘 다 **해소됨**
- 저장 포맷 **신설 안 함** — GSD/HANDOFF/`wip-save` 중 상황에 맞는 것에 위임
- 역할: `researcher`(sonnet, 외부 조사) 신설 / `analyzer`(haiku) 로컬 전담 / `architect` 웹도구 제거

## 검증 완료

> 1~4라운드 행은 조언자가 직접 재현. **5라운드 3행(재래치·심링크·`input` 앵커)은 worker 실행 결과이며
> Codex 재검증 전이다.** 재현 하네스는 `mktemp -d` 스크래치에서 렌더마다 별도 프로세스로 실행했다
> (실행 중 세션의 실제 `claude-ctx-*` 파일과 격리).

| 항목 | 결과 |
|---|---|
| 브리지 경계값 | delta 51,132→36 / **51,133→35**(WARNING) / 58,999→26 / **59,000→25**(CRITICAL) / 78,666→0 (clamp) |
| **compact 재래치 (prev 기반)** | 50,905→100 · 109,905→25 · **62,000→100**(하락 감지·재래치·`-warned.json` 삭제) · 113,000→36. 구버전 코드로 같은 시퀀스: 62,000→**86**, 113,000→**22** (RED) |
| ↳ 실제 `awesome-statusline.sh` 로 재현 | 같은 4단계를 **설치된 실파일**에 stdin 을 흘려 실행 → 100/25/100/36, ③에서 `-warned.json` 삭제, **전 렌더 stderr 0바이트**. 설치된 블록 == install.sh heredoc (`diff` 0) |
| 심링크 statusline | 상대 심링크(`sl.sh → real/actual.sh`) 대상 설치 후에도 `test -L` 참, 마커는 **실제 파일**에 1개, 퍼미션 755 보존. 구버전: 심링크가 일반 파일로 대체됨(RED) |
| `input` 앵커 | `payload=$(cat)` 스크립트에 설치 시도 → 경고 후 skip, **md5 무변경**. 구버전: 삽입돼 매 렌더 stderr `input: unbound variable` + 브리지 파일 0건(기능 사망) |
| **구 블록 정리 (업그레이드 경로)** | 구 버전이 `payload=$(cat)` 스크립트에 심어둔 블록으로 시작(마커 1, `unbound variable` 재현) → 새 설치기 실행 → **마커 0 · unbound 0 · 원본과 바이트 동일 · 백업 생성 · 퍼미션 755 · tmp 잔여 0**. RED(정리 호출 1줄 제거): 마커 **1 유지**, unbound **재현** |
| ↳ 정리 경로 안전 가드 | `start` 만 있고 `end` 없음(1/0) → 경고 후 skip, **md5 무변경 · 사용자 코드 생존**(가드 없으면 블록 시작 이후 전부 삭제됨). 정상 마커 **2쌍**(2/2) → 동일하게 skip, md5 무변경, 줄수 11→11 |
| **수동 수리본 보존 (3-way)** | (a) 구 블록(`$input`) → **제거**(마커 0·백업 1·블록 없는 원본과 `cmp` 일치) / (b) 수리본(`$input`→`$payload`) → **보존**(마커 1·md5 무변경·렌더 시 unbound 0·브리지 json 생성) / (c) 블록 없음 → 경고만·md5 무변경. **RED**(가드 제거): (b) 가 **파괴됨** — 마커 0·md5 변경·브리지 json 0(경고 기능 사망) |
| ↳ 판정 범위·표기 | 마커 **밖**에만 `$input` 이 있는 수리본 → **보존**(범위 한정 확인) / 블록 안 `${input}` 중괄호 표기 → **제거**(두 표기 모두 인식) |
| **선언 형태 매트릭스** | 구 블록 심은 상태로 설치 → `input=$(cat)` **마커 1** / `export input=$(cat)` **마커 1**(삭제 안 됨) / `  input=$(cat)`(선행 공백) **마커 1** / `payload=$(cat)` **마커 0·백업 1**(제거). **RED**(좁은 `^input=`): `export`·선행공백 케이스가 **마커 0** — 동작하던 브리지가 삭제됨 |
| ↳ 앵커 정규식 단위 | MATCH: `input=` `export input=` `  input=` `declare -x input=` `local input=` `typeset input=` / no: `payload=` `# input=주석` `sinput=` `myinput=` |
| **Red-Green 종단 주입** | GREEN: `CONTEXT CRITICAL` 실제 주입 / RED: 삭제 시 소멸. ※ **구 GSD 훅 메시지**(당시 배선). 현행 문자열은 `CONTEXT BUDGET WARNING`/`CONTEXT BUDGET CRITICAL` |
| `set -e` 회귀 | GREEN `OK` 생존 / RED(`\|\| true` 제거) 출력 `[]` 사망 — **픽스처 기준.** 실환경 `awesome-statusline.sh` 자체에는 `set -` 선언이 없어(확인함) 이 방어는 다른 사용자의 `set -e` statusline 대비용이다 |
| statusLine 5형태 | `~/…`, `bash ~/…`, `env FOO=1 ~/…`, `bash $HOME/…`, `${HOME}/…` 전부 마커 1개 |
| 원자성 | 퍼미션 755 보존, tmp 잔여 0, **rename 실패 시 원본 md5 동일 + exit 1** |
| 멱등성 | 픽스처: 미설치→삽입→재실행 `diff` 0, 마커 1, 마커 밖 커스터마이징 보존. **실환경**: 재실행 시 `변경 없음 → 건너뜀`, md5 동일, 새 `.bak` 0개, 마커 1, `settings.json` md5 무변경 |
| 실전 발동 | 2026-07-27 세션에서 WARNING 자연 발동 확인 |

## Codex 검증 이력

- 1라운드 P2×2: 래퍼 커맨드 파싱 / `set -e` 사망 → 반영·재현 확인
- 2라운드 P2×2: 비원자적 덮어쓰기 / `$HOME` 미확장 → 반영·재현 확인
- 3라운드: 결과 미기록 / 4라운드: 프로세스 사망(툴 실패, 코드 결함 아님)
- 5라운드 P0/P1×3: compact 재래치 무력화 / 심링크 파손 / `$input` 변수명 가정 → 반영 완료(아래), 조언자 재현 검증 통과
- 6라운드 P1×1: `^input=` skip 이 구 버전의 깨진 블록을 방치(업그레이드가 고장을 못 고침) → 반영 완료(아래), 조언자 재현 검증 통과
- 7라운드 P2×1: 정리 경로가 사용자의 수동 수리본까지 파괴 → 반영 완료(아래), 조언자 3-way 재현 검증 통과
- **8라운드 ×1: `^input=` 앵커가 `export input=` 등 유효 선언을 놓쳐 동작 중인 브리지를 삭제 → 반영 완료(아래), 재검증 대기**

## 다음 단계

1. **Codex 재검증** — `node ~/.claude/plugins/cache/openai-codex/codex/1.0.5/scripts/codex-companion.mjs review --scope working-tree`
   (5~8라운드 반영분은 워킹트리 미커밋 상태. 브랜치 전체를 볼 때는 `--scope branch --base main`)
2. 통과 시 `TaskStop ctx-budget-fix5`
3. 커밋 (사용자 지시 대기 중). 제안 메시지:
   `fix(bundle): compact 재래치를 직전 관측값 기준으로 교정 + 심링크/입력변수 앵커 보강`

## Codex 5라운드 지적 3건 — 반영 완료 (미커밋)

착수 확인용: `grep -cE 'ctx_prev|readlink|\^input=' install.sh` 가 0 이면 미착수.

1. **[최우선] compact 후 예산이 리셋되지 않던 결함.** 재래치 조건이 `current < __ctx_base` 였는데
   compact 후 토큰 수는 `baseline + 요약` 이라 baseline 보다 커서 조건이 영원히 거짓이었다.
   → `…-prev` 파일로 직전 관측값을 추적하고 **값이 하락하면** 재래치(+`-warned.json` 삭제).
   회귀는 baseline **보다 큰** 하락값(62,000)으로 재현했고 구버전과 값이 갈린다(검증표 참조).
2. **심링크 statusline 파손.** → 쓰기 전에 `readlink` 로 최대 5홉 해석(상대 심링크는 대상
   디렉토리 기준 결합), 해석 실패·5홉 초과·비정규파일이면 경고 후 skip.
3. **`$input` 변수명 가정.** → 대상 선정 조건에 `^input=` 앵커를 AND 로 추가. 두 앵커를 모두
   가진 파일만 대상. `CURRENT_USAGE=` 만 있는 경우 사유를 명시한 경고 후 skip(파일 무수정).

브리프 의사코드에서 **의도적으로 벗어난 1건**: 최초 관측(base/prev 없음) 경로에서도 `-warned.json`
을 지운다. 브리프는 하락 경로에만 명시했으나, 구버전(`base -eq 0` 분기)이 이미 지우고 있어
생략하면 조용한 동작 회귀가 된다. 두 경로를 `__ctx_relatch` 플래그로 합쳐 비대칭을 없앴다.

## Codex 6라운드 지적 1건 — 반영 완료 (미커밋)

착수 확인용: `grep -c 'ctx_cleanup_stale_block "\${ctx_partial}"' install.sh` 가 0 이면 미착수.
(정의가 아니라 **호출**을 센다 — 호출 1줄만 지운 상태가 바로 아래 RED 조건이다.)

**지적 D — `^input=` 앵커 skip 이 구 버전의 깨진 블록을 방치한다.** 구 버전은 `^CURRENT_USAGE=`
앵커만 봤으므로 stdin 을 `payload=$(cat)` 등으로 받는 스크립트에도 블록을 심었고, 그 블록은
`$input` 을 참조해 매 렌더 죽는다. 5라운드 수정으로 그런 파일이 **대상에서 제외되면서**
재실행해도 경고만 뜨고 깨진 블록이 그대로 남았다 — 업데이트가 고장을 못 고치는 상태.

→ skip 하기 전에 `ctx_cleanup_stale_block` 을 호출한다: 마커가 있으면 **백업 후 블록만 제거**하고
(마커 밖 100% 보존) 안내한다. **블록을 새로 삽입하지는 않는다**(skip 유지).
후보로 잡힌 파일(= `^CURRENT_USAGE=` 보유)에만 적용하므로, 스크립트를 아예 못 찾은 경우엔 무동작.

**새 경로를 만들지 않고 공용 헬퍼로 추출해 양쪽이 같은 코드를 탄다** (지시대로):

| 헬퍼 | 역할 | 사용처 |
|---|---|---|
| `ctx_resolve_link` | 심링크 5홉 해석 + 5홉초과/비정규파일 거부 | 삽입 · 정리 |
| `ctx_markers_sane` | 마커 0쌍 또는 정상 1쌍만 통과 | 삽입 · 정리 |
| `ctx_strip_markers` | 마커 블록만 제거 | 삽입(재삽입 전) · 정리 |
| `install_file_preserve_mode` | 백업 + `cp -p`/`mv` 원자적 교체 | 삽입 · 정리 (기존) |

`ctx_markers_sane` 를 정리 경로에도 태운 것이 핵심 안전장치다 — `start` 만 있고 `end` 가 없는
손상 파일에 strip 을 돌리면 `inblock` 이 끝까지 유지돼 **블록 시작 이후 사용자 코드가 통째로
사라진다.** 해당 케이스는 경고 후 무동작임을 회귀로 확인했다(검증표 참조).

## Codex 7라운드 지적 1건 — 반영 완료 (미커밋)

착수 확인용: `grep -c 'if ! ctx_block_uses_input' install.sh` 가 0 이면 미착수.
(6라운드와 동일하게 정의가 아니라 **호출**을 센다 — 정의만 남기고 호출을 지운 상태가 아래 RED 조건이다.)

**지적 E — 정리 경로가 "수동 수리된 블록"까지 지운다.** 6라운드에서 추가한 경고는 사용자에게
**수동 삽입을 안내**한다(REVIEW §4). 즉 `payload=$(cat)` 스크립트에서 사용자가 블록의 `$input` 을
자기 변수명으로 고쳐 정상 동작시키는 것은 **지원되는 시나리오**다. 그런데 정리 경로가 **마커 존재만**
보고 지우면, 재실행할 때마다 잘 돌던 수리본을 파괴하고 그 환경의 컨텍스트 경고를 꺼버린다.

→ `ctx_block_uses_input` 추가: **마커 사이 본문이** `$input` / `${input}` 을 참조할 때만 제거한다.
참조가 없으면 수동 수리본으로 보고 보존 + 안내. 마커가 없으면 지금처럼 경고만.

설계 요점 2가지:

- **판정 범위를 마커 사이로 한정한다.** 파일 어디든 `input` 문자열이 있으면 반응하는 구현은
  사용자가 주석에 `$input` 을 적었다는 이유로 정상 블록을 파괴한다. `ctx_strip_markers` 의 awk 를
  뒤집어(버리는 대신 출력) 본문만 뽑는다 — 검증표의 "마커 밖에만 `$input`" 행이 이 경계를 친다.
- **두 표기를 모두 잡는다.** `\$\{input\}|\$input([^A-Za-z0-9_]|$)` — `${input}` 을 놓치면
  깨진 블록이 수리본으로 오인돼 영구히 남는다. 경계문자 조건은 `$input_data` 같은 다른 변수에
  오탐하지 않기 위한 것이다.

마커는 **소유권**을 말하지 **건강 상태**를 말하지 않는다 — 자동 정리의 판정 기준을 위치 표식에서
내용 속성으로 옮긴 수정이다.

## Codex 8라운드 지적 1건 — 반영 완료 (미커밋)

착수 확인용: `grep -c '^CTX_INPUT_ANCHOR=' install.sh` 가 0 이면 미착수.

**지적 F — `^input=` 앵커가 유효한 선언 형태를 놓친다.** `export input=$(cat)` 처럼 `$input` 이
**정상 정의되는** 스크립트는 구 버전이 심은 브리지가 잘 돌고 있는데, 좁은 앵커가 이를 "미지원" 으로
분류하고 6라운드의 정리 로직이 **동작하던 브리지를 삭제**했다. 업그레이드가 멀쩡한 기능을 끄는 셈.

→ 앵커를 단일 정의 `CTX_INPUT_ANCHOR` + `ctx_has_input_anchor()` 로 뽑고 범위를 넓혔다:

```
^[[:space:]]*(export[[:space:]]+|declare[[:space:]]+-[a-zA-Z]+[[:space:]]+|local[[:space:]]+|typeset[[:space:]]+)?input=
```

**왜 한 곳만 고치면 두 문제가 닫히는가:** 이 판정 하나가 분기를 가른다 — 통과하면 '삽입 대상',
실패하면 '정리 경로'다. `ctx_partial` 은 앵커 실패의 **부산물**이므로, 앵커가 좁으면 정상 파일이
정리 대상으로 흘러들어간다. 그래서 정의는 반드시 한 곳뿐이어야 하고(두 곳에 쓰면 갈라진다),
하드코딩된 `'^input='` 은 코드에 0건 남아 있다.

7라운드의 `$input` 참조 기반 수리본 보존 로직은 **그대로 유지**된다(3-way 무회귀 확인).

5~8라운드는 모두 **앵커/판정 정밀도**라는 한 계열이었다: 앵커를 좁히면 정상 파일이 정리 경로로
새고(F), 정리를 넓히면 수리본을 파괴한다(E). 판정을 단일 정의로 모은 것이 계열 전체의 봉합이다.

## 2026-07-29 전환 — 번들 전용 컨텍스트 훅 (GSD 의존 제거)

브랜치 `feat/ctx-own-hook` · **미커밋**(Codex 검증 대기).
착수 확인용: `grep -c 'advisor-context-budget' install.sh` 가 0 이면 미착수.

**바뀐 것: 누가 훅을 소유하는가.** 임계 수치(51,133 / 59,000)는 **그대로**다 — 소유권 이전과
임계치 재조정을 동시에 하면 회귀 원인을 가릴 수 없다.

| 항목 | 이전 | 이후 |
|---|---|---|
| 브리지 파일 | `claude-ctx-{sid}.json` | `claude-ctx-advisor-{sid}.json` (상태·디바운스 파일도 동일 접두사) |
| 기록 내용 | 합성 `remaining_percentage`/`used_pct` (`BUDGET_RAW=78666` 역산) | 사실값 `baseline/current/delta/window/window_pct` |
| 판정 주체 | `gsd-context-monitor.js` (GSD 플러그인 소유) | `advisor-context-budget.js` (**번들 소유**) |
| `settings.json` | 무수정 | `hooks.PostToolUse` 에 멱등 병합 (백업 + 유효성 게이트) |
| 경고 문구 | `Usage at X%. Remaining: Y%.` (숫자가 예산 소진율) | 예산 델타와 창 사용률을 **분리 표기** + "고갈 아님" 명시 |

해소: **R1**(플러그인 내부 의존) · **R2**(GSD config 로 무력화 가능) · **R3**(숫자 의미 왜곡).
새로 진 위험: **R3b** — 설치기가 살아있는 `settings.json` 에 쓴다(§7 규율표로 방어).

**GSD 훅은 건드리지 않았다.** 브리지 파일명이 달라져 `existsSync` 가드에서 종료 = 이 기능
도입 **이전의 원래 휴면 상태**로 자연히 복귀. `settings.json` 의 GSD 엔트리는 그대로 둔다.

**동반 처리 2건**
- `hooks/universal/contextMonitor.js`(Stop) **조건부 제거** — `commands/save-and-compact.md` 가
  없음을 기계적으로 확인했을 때만. 그 훅은 `stopEvent.js`·`cleanup-bg-shells.sh` 와 **배열 원소를
  공유**하므로 그룹째가 아니라 **내부 훅 단위**로 걷어낸다(그룹째 지우면 무관한 훅 2개 동반 삭제).
- `~/.claude/skills/external-memory/SKILL.md` 의 죽은 트리거 "Token Threshold (150K)" →
  Context Budget Trigger 로 교체 (1회성 직접 수정 — 번들 소유 파일이 아니라 설치기에 넣지 않았다).

**제거한 것:** `GSD_HOOK_PIN` / `check_gsd_hook_version()`. GSD 내부 구현에 의존하지 않게 되어
버전 대조가 무의미해졌고, 남겨두면 "브리지를 써도 경고가 주입되지 않습니다" 같은 **거짓 경고**를
낸다. 이번 변경이 만든 사문화이므로 surgical-changes 원칙의 "자기 변경이 만든 것만 정리"에 해당한다.

### 검증 (worker 실행 · Codex 재검증 전)

| 항목 | 결과 |
|---|---|
| 문법 | `bash -n install.sh` PASS · `node --check advisor-context-budget.js` PASS |
| **settings.json 보존** | 실 설정 사본으로 설치 → 훅 이벤트 **12종 전부 유지**(브리프의 "14종"은 부정확 — 실측 12종), Orca 엔트리 40건 유지, **정규화 JSON 깊은 비교 완전 일치**(추가/제거 대상만 걷어냄). 파싱→덤프 왕복이라 파일 바이트 동일은 원리적으로 불가 — 이 행은 **JSON 문서 동일성**을 말한다 |
| ↳ 내부 훅 단위 제거 | contextMonitor 제거 후 **`stopEvent.js`·`cleanup-bg-shells.sh` 생존** 단언 통과 |
| 멱등 | 2회 실행 시 `변경 없음 → 건너뜀`, 결과 `diff` 0, 우리 엔트리 **1개**, 백업 1개(== 원본) |
| **유효성 게이트 (RED-GREEN)** | GREEN(픽스처로 mv 직후 파일 파손): 백업 복원 + `exit 1` + 배너 미출력, 복원본 훅 12종. RED(게이트 무력화): **깨진 채 잔존 · exit 0 · 배너 출력**. ※ 정상 경로(파싱→덤프)는 항상 유효한 JSON 을 내므로 이 분기는 **실사용에서 도달하지 않는다** — 잘린/중단된 쓰기 대비 안전망이며 발동은 픽스처로만 확인했다 |
| **구 파일 정리 (RED-GREEN)** | advisor 파일이 **먼저** 생긴 뒤 구 파일이 나타나는(재래치 없는) 순서로 검증. GREEN(무조건 정리): 구 브리지 제거·advisor 파일 무사·GSD 무출력. RED(재래치 경로에만 정리 = 수정 전): 구 브리지 **잔존**, GSD 훅이 `CONTEXT CRITICAL: Usage at 80%. Remaining: 20%.` **실제 주입** |
| 훅 임계·디바운스 | 51,132→무출력 / 51,133→WARNING / 직후 2회 억제 / 59,000→**승격 즉시 CRITICAL** / 직후 4회 억제 / 6회째 재발화 |
| ↳ 카운터 비소모 | 임계 미만에서는 `-warned.json` 생성 안 됨(경고 이력 없는 세션의 디바운스 소진 방지) |
| 방어 경로 | 브리지 없음 / 파싱 실패 / stale / stdin 비JSON / session_id 없음 / delta 비수치 → **전부 무출력 + exit 0** |
| 메시지 | CRITICAL·WARNING 둘 다 delta 실수치 + 창 사용률 + "고갈 아님" 포함. `window_pct` 결측 시 "창 사용률은 미상" 폴백 |
| statusline 사실 기록 | 50905→delta 0 / 102038→**delta 51133** / 109905→**delta 59000** / 62000→재래치 후 delta 0. `remaining_percentage`·`used_pct` **필드 없음**. 전 렌더 stderr 0바이트 |
| ↳ 창 무관 | 같은 delta 59,000 이 200k 창에서 `window_pct 54`, 1M 창에서 `10` — 판정은 동일 |
| **GSD 휴면** | 미리 깔아둔 구 파일 4종이 첫 렌더에서 정리됨, `claude-ctx-{sid}.json` 미생성, 실제 `gsd-context-monitor.js` 에 먹여도 **무출력** |
| contextMonitor 조건부 | (a) `save-and-compact.md` 없음 → 제거 + 안내 / (b) 픽스처로 생성 → **제거 안 함**. 둘 다 확인 |
| 기존 회귀 | 마커 1개 · 2회 실행 `diff` 0 · 마커 밖 보존 · 퍼미션 755 · tmp 잔여 0 · 선언 형태 4종(`input=`/`export input=`/선행공백/`payload=`) · 3-way 정리(구 블록 제거·수리본 보존·블록 없음 무동작) · 심링크 유지 + 실제 파일에 마커 · 마커 구조 비정상 시 md5 무변경 |
| 설치 블록 == heredoc | `diff` 0 |
| `jq` 폴백 경로 | `python3` 만 제거한 PATH 로 실행 → python3 경로와 동일 결과(엔트리 1·contextMonitor 내부 훅만 제거·stopEvent/cleanup 생존·2회차 `diff` 0) |
| 둘 다 없음 | 경고 후 skip, `settings.json` md5 무변경, 설치는 `exit 0` 으로 계속 |

### Codex 9라운드 지적 3건 (G/H/I) — 반영 완료

착수 확인용: `grep -c 'ms_node=' install.sh` 가 0 이면 미착수.

세 건 모두 **statusline 경로가 이미 가진 보호를 settings.json 경로가 재사용하지 않아** 생긴,
같은 계열의 결함이었다. 새 기법이 아니라 기존 헬퍼를 끌어다 썼다.

**지적 G [P1] — 하드코딩 PATH 가 사용자 node 를 못 집는다.** 등록 커맨드가
`PATH=/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin node …` 였다. nvm·Volta·`~/.local/bin` 에만
node 가 있으면 매 도구 호출 **exit 127**, 있으면 **다른 바이너리로 조용히 실행**된다. 게다가
설치기의 `command -v node` 검사는 원래 PATH 로 성공하므로 "node 있음"이라 보고하면서 훅만 죽는다.

> **이 워크스페이스 실측(조용한 실패 쪽이었다):**
> `command -v node` = `~/.local/bin/node` **v22.23.1** vs 하드코딩 PATH = `/usr/local/bin/node` **v25.2.1**

→ 설치 시점에 `command -v node` 로 **절대경로를 해석해 박는다**. `PATH=` 접두사는 **제거**(절대경로가
있으면 불필요하고, 있으면 해롭다). node 부재 시 훅 등록만 skip 하고 설치는 계속한다.
멱등 판정은 커맨드 전체가 아니라 **훅 파일 경로 부분문자열**로 하고, 이미 있으면 **커맨드를 갱신**한다
— 전체 문자열로 대조하면 node 경로가 바뀐 환경에서 같은 훅이 중복 등록되고, 갱신하지 않으면
업그레이드가 낡은 node 경로를 못 고친다.

**지적 H [P1] — 제한적 퍼미션이 넓어진다.** python 이 `open()` 으로 만든 임시본은 umask(022)를 타
0644 가 되고, 그대로 `mv` 하면 0600 이던 `settings.json` 이 0644 로 넓어진다. 이 파일은 `env` 키를
담을 수 있어(실측: `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`) 자격증명이 다른 로컬 사용자에게 노출될 수 있다.
→ statusline 과 **같은 헬퍼 `install_file_preserve_mode`** 로 교체한다(`cp -p` 퍼미션·소유 승계 +
같은 디렉토리 임시본 + 원자적 `mv` + 백업).

> **부수 발견(중요):** 그 헬퍼는 내부적으로 `"${target}.tmp.$$"` 를 쓰는데, 병합 임시본이 같은
> 이름이면 헬퍼가 **우리 결과를 원본으로 덮어쓴 뒤 자기 자신을 `cat` 해 빈 파일**을 만든다.
> 병합 임시본을 `mktemp` 로 옮겨 충돌을 없앴다.

**지적 I [P2] — settings.json 심링크를 일반 파일로 갈아치운다.** dotfiles 저장소 연결이 끊긴다.
→ statusline 이 쓰던 **`ctx_resolve_link`(5홉 해석)를 그대로 재사용**한다. 복사하지 않고 함수를
공유하므로 같은 계열 결함이 경로마다 따로 재발하지 않는다.

| 항목 | 결과 |
|---|---|
| 문법 | `bash -n install.sh` PASS · 하드코딩 `PATH=/usr/local/bin` 코드 **0건** |
| **G 등록 커맨드** | `"/Users/…/.local/bin/node" "/Users/…/advisor-context-budget.js"` — `PATH=` 접두사 **없음**, 절대경로. 그대로 실행 시 **exit 0·무출력**(브리지 없음), 실행 node **v22.23.1 = 사용자 node 일치**(수정 전 v25.2.1) |
| ↳ node 부재 | 제한 PATH 로 실행 → 훅 등록 skip + 경고, **설치는 exit 0 으로 계속**(배너 출력), 엔트리 미등록 |
| ↳ 낡은 엔트리 갱신 | `PATH=…` 커맨드를 심어두고 재실행 → `~ 커맨드 갱신` 출력, 절대경로로 교체, **엔트리 1개**(중복 아님) |
| **H 퍼미션 (RED-GREEN)** | GREEN: `600 → 600` 보존 / RED(수정 전 재현: `${target}.tmp.$$` + 직접 `mv`): **`600 → 644` 넓어짐** |
| **I 심링크 (RED-GREEN)** | GREEN: 심링크 유지 ✓ · 실제 파일 갱신 ✓ / RED(해석 제거): **심링크 끊김 · 실제 파일 미갱신** |
| 멱등 | 2회 설치 `diff` 0 · 엔트리 1개 · 정규화 비교 유실 0 |
| 회귀 | 디바운스 5회 간격 · 임계 51,133/59,000 · statusline 사실 기록 · contextMonitor 양방향 · 마커/심링크/원자성 전부 유지 |
| 실환경 | 낡은 `PATH=` 엔트리가 **갱신**됨(추가 아님), 퍼미션 `644 → 644` 유지, 훅 이벤트 12종 유실 0, 종단 체인 WARNING/CRITICAL 발화 |

### Codex 10라운드 지적 1건 (J) — 반영 완료

착수 확인용: `grep -c 'if merge_settings_hooks; then' install.sh` 가 0 이면 미착수.

**지적 J [P2·결과는 심각] — 등록 실패 시 컨텍스트 경고가 전부 꺼진다.** 순서가
'브리지 전환 먼저 → settings.json 병합 나중'이었고, 병합에는 **경고만 하고 정상 반환하는 경로가
여럿**이었다(심링크 5홉 초과, `python3`/`jq` 부재, 유효성 게이트 등). 그 경우:

```
statusline → claude-ctx-advisor-*.json 만 쓰고 구 메트릭까지 삭제
settings   → advisor 훅 미등록, GSD 훅만 잔존
GSD 훅     → claude-ctx-*.json 을 찾는데 아무도 안 쓴다 → 조용히 종료
결과       → 설치는 "성공"으로 끝나고 감지 수단만 사라진다
```

**이 작업이 처음부터 고치려던 실패 양식(REVIEW §1.2 "감지 수단 없는 규칙은 사문화된다")을
설치기가 스스로 만든다.** → 순서를 뒤집고 실패를 전파했다.

- `merge_settings_hooks` 에 **반환값 계약**을 부여: `0` = 훅이 실제로 등록됨 / `1` = 미등록(+`MS_REASON`)
- 판정은 분기 플래그가 아니라 **사후 조건**(`ctx_hook_registered` — 결과 파일을 직접 확인)으로 한다.
  분기마다 성공 플래그를 세면 새 분기가 추가될 때 판정이 따라오지 못한다 — **이 지적의 원인이 그것**이었다
- **훅 등록 성공했을 때만** `install_ctx_bridge` 를 호출한다. 실패하면 statusline 을 건드리지 않으므로
  업그레이드면 구 브리지가 계속 동작하고, 신규 설치면 원래 아무것도 없었으니 회귀가 없다
- 구 메트릭 `rm -f` 는 새 브리지 본문 안에 있으므로 **전환과 자동으로 같은 조건**이 된다
- 부분 실패(훅 등록됨 + contextMonitor 제거 실패 등)는 전환을 막지 않는다 — 사후 조건이 훅 등록만 본다

| 항목 | 결과 |
|---|---|
| 문법 | `bash -n install.sh` PASS · 모든 `return 1` 경로에 `MS_REASON` 세팅 확인(감사 출력) |
| **(a) 6홉 심링크 (RED-GREEN)** | GREEN: statusline `claude-ctx-advisor` 참조 **0건**(구 형식 유지) · 훅 미등록 · exit 0 · 경고 출력 · **구 브리지 렌더 시 `claude-ctx-RT.json` 생성**(GSD 훅 계속 동작) / RED(수정 전 순서): 참조 **4건**(전환됨) · 훅 미등록 · 경고 **0** · **`claude-ctx-RT.json` 미생성, advisor 파일만 생성 = 경고 전면 소실** |
| (b) `python3`/`jq` 부재 | GREEN: 전환 안 함 + 사유 출력 + 구 브리지 동작. RED 도 전환되지 않음 — 그 환경에선 `install_ctx_bridge` 자체가 `jq` 부재로 조기 종료하기 때문이다. **결함을 가르는 것은 (a)** 이며 (b)는 무해함 확인용이다 |
| (c) 정상 경로 무회귀 | 훅 등록 YES · 브리지 전환(참조 4건·마커 1) · contextMonitor 제거 · 구 메트릭 정리 배선 1건 |
| 회귀 | 디바운스 5회 간격 · 임계 51,133/59,000 · 퍼미션 `600→600` · 심링크 유지 · 멱등 엔트리 1개 · 정규화 유실 0 |
| 실환경 | 재실행 `변경 없음 → 건너뜀`(멱등) · 퍼미션 `644→644` · 마커 1 · 훅 12종 유실 0 · 등록 커맨드 절대경로 node |

> **테스트 자체의 함정 2건(기록):** ① 지적 I 의 RED mutant 가 문자열 고정이라 코드가 바뀌자
> 조용히 **no-op 복사본**이 되어 RED 가 거짓 통과했다 → 정규식 + `assert n == 1` 로 못 박았다.
> ② 지적 H 의 RED 를 `mktemp`(0600)로 만들면 결함이 안 드러난다 → 수정 전 코드(python `open()` +
> umask)를 정확히 재현해야 `600→644` 가 나온다. **RED 가 통과하면 RED 자체를 먼저 의심할 것.**

### Codex 11라운드 지적 1건 (N) — 반영 완료 · 계열 종결

착수 확인용: `grep -c 'ctx_hook_entry_count' install.sh` 가 0 이면 미착수.

**지적 N — 쓰기 실패가 성공으로 통과해 브리지가 전환된다.** `install_file_preserve_mode` 가 cp/mv
실패에도 `return 0` 했고, 성공 판정은 "엔트리에 `advisor-context-budget.js` **가 들어있는가**"라는
**부분일치**였다. 업그레이드에서 낡은 node 경로 갱신이 실패해도 성공으로 끝나
**실행 불가능한 훅 + 전환된 브리지 = 경고 전면 중단**(J와 같은 결과, 한 층 안쪽).

**인스턴스가 아니라 계열을 닫았다 — 원칙: 쓰기 결과를 믿지 말고 읽어서 확인한다.**

1. **실패 전파.** `backup_if_exists`·`install_file`·`install_file_preserve_mode` 의 모든 실패 경로가
   비0 을 반환한다. `set -e` 하에서 호출부가 중단되지 않도록 **전 호출부를 명시 처리**했다.
2. **사후 조건을 완전일치로.** 쓴 뒤 다시 읽어 ⓐ유효 JSON ⓑadvisor 엔트리 **정확히 1개**
   ⓒ커맨드가 `ms_cmd` 와 **문자열 완전 일치** 를 모두 만족할 때만 성공. 어긋나면 백업 복원 → 실패 반환
   → 브리지 전환 금지. 판정 결과를 `exact:1`/`found:0`/`dup:N`/`stale:1` 로 구분해 사유를 남긴다.
3. **탐색용 부분일치는 유지.** 기존 엔트리를 '찾는' 것은 파일명 부분일치가 맞다(node 경로가 바뀌어도
   찾아야 갱신할 수 있다). 바꾼 것은 **"성공했는가" 판정**뿐이다.
4. `install_ctx_hook` 도 쓰기 실패 전파 + **재읽기 검증**(파일 존재 + 내용에 `CONTEXT BUDGET CRITICAL`)
   을 하고, 메인 흐름을 `if install_ctx_hook && merge_settings_hooks` 로 묶었다 —
   훅 파일이 없으면 등록해도 실행 불가능한 훅이 되기 때문이다.

**조용한 성공 반환 경로 감사 (전수)**

| 위치 | 이전 | 이후 |
|---|---|---|
| `backup_if_exists` cp 실패 | 무검사(=set -e 중단 또는 백업 없이 진행) | 경고 + `return 1` |
| `install_file` mv 실패 | 무검사 | 경고 + `return 1` |
| `install_file_preserve_mode` 백업/cp/cat/mv 실패 4곳 | 전부 `return 0` | 전부 `return 1` |
| `merge_settings_hooks` 쓰기 | 반환값 무시 | `if ! …; then MS_REASON 설정 후 return 1` |
| `merge_settings_hooks` 성공 판정 | 부분일치 존재 여부 | **완전일치 + 개수 1 + 유효 JSON** |
| `install_ctx_hook` | 반환값 없음 | 전파 + 재읽기 검증 |
| 호출부 — CLAUDE.md·agents 4종·reviewer.md·`install_ctx_bridge`·`ctx_cleanup_stale_block` | 무검사 | 각각 명시 처리(경고 후 계속 / 백업 실패 시 덮어쓰기 금지) |

| 항목 | 결과 |
|---|---|
| 문법 | `bash -n install.sh` PASS |
| **업그레이드 갱신 GREEN** | 낡은 `"/nonexistent/node"` 엔트리 → 실제 node 절대경로로 **갱신**, 엔트리 **1개**, 브리지 전환 4건 |
| **쓰기 강제 실패 (`chmod 555`)** | exit 1 · settings **md5 무변경** · 브리지 **미전환** · 경고 출력. ※ RED 는 `install_ctx_hook` 게이트가 먼저 잡아 구분되지 않는다 — 이 결함을 가르는 것은 아래 변조 케이스다 |
| **재읽기 완전일치 (RED-GREEN·결정적)** | 병합 직후 커맨드를 외부 변조하는 픽스처. GREEN: `훅 엔트리의 커맨드가 기대값과 다릅니다` → **백업 복원 · 브리지 0건(전환 금지)** / RED(부분일치 판정으로 되돌림): 통과해버려 **브리지 4건 전환 = 실행 불가 훅 + 전환된 브리지** |
| 중복 엔트리 | 사전 2개 → `훅 엔트리가 2 개 중복 등록됐습니다` · 브리지 0건 |
| 회귀 | (a)/(b) 차단·대조군 전환+등록 · 디바운스 5회 · 51,133/59,000 · 퍼미션 `600→600`(RED `600→644`) · 심링크 유지(RED 끊김) · 멱등 1개 · 정규화 유실 0 · 임시파일 0(RED 2개) · 마커 1 · 3-way |
| 실환경 | 재실행 `변경 없음`(멱등) · settings/CLAUDE.md md5 무변경 · 퍼미션 644 유지 · 훅 12종 · mktemp 잔여 0 · 등록 커맨드 절대경로 node |

> **RED mutant 드리프트 — 이번까지 4회.** 코드가 바뀌면 문자열 고정 mutant 가 조용히 no-op 이 되어
> **RED 가 거짓 통과**한다(이번엔 퍼미션 RED 가 `600→600` 으로 보였다). 모든 mutant 에
> `assert <대상> in s` 를 넣어 대상이 사라지면 **테스트가 실패하도록** 못 박았다.
> **RED 가 통과하면 RED 자체를 먼저 의심할 것.**

### Codex 12라운드 지적 2건 (O/P) — 반영 완료 · **조언자 검증 전 (다음 세션 인계)**

착수 확인용: `grep -c 'merge_settings_hooks cleanup' install.sh` 가 0 이면 미착수.

> **⚠ 이 절은 조언자 예산 소진으로 재현 검증을 받지 못했다.** 아래 수치는 **worker 실행 결과**다.
> 다음 세션은 `/codex:review --scope working-tree` 부터 시작할 것.

**지적 O [P1] — 대체 브리지 확인 전에 구 모니터를 제거한다.** `contextMonitor.js` 제거가
`install_ctx_bridge` 보다 먼저 실행됐다. 브리지 삽입은 skip/실패해도 계속 진행되므로
(`jq` 없음·`statusLine` 없음·앵커 불충족·쓰기 실패) **구 모니터는 사라졌는데 새 훅이 읽을 메트릭이
없는** 상태가 만들어진다. J·N 과 같은 계열의 세 번째 변형.

→ `merge_settings_hooks` 를 **모드화**했다: `register`(등록 전용, 제거 안 함) / `cleanup`(구 모니터
제거 전용). `install_ctx_bridge` 는 **성공/skip 을 반환값으로 구분**하고(사유는 `CTX_BRIDGE_REASON`),
성공 시에만 `cleanup` 이 돌아간다. 브리지도 **사후 재읽기 검증**(마커 존재 + `claude-ctx-advisor-`
문자열 존재)을 통과해야 성공이다.

```
if install_ctx_hook && merge_settings_hooks register; then
  if install_ctx_bridge; then merge_settings_hooks cleanup
  else  "브리지가 전환되지 않아 구 Stop 모니터를 그대로 둡니다 / 사유: …"  fi
else …
```

**지적 P [P2] — 구 메트릭 정리가 사용량 가드 안쪽.** 업그레이드 직후 첫 렌더가
`CURRENT_USAGE:null` 또는 토큰 합 0 이면 정리에 도달하지 못하고, GSD 훅이 남은
`claude-ctx-{sid}.json` 을 TTL(60초) 동안 읽어 **이번 마이그레이션이 없애려던 낡은 경고를 주입**한다.
→ `session_id` 획득 직후, 사용량 가드보다 **앞**으로 옮겼다(정확한 이름 4종만 `rm -f`, 글롭 금지).
실측: 설치된 블록에서 정리 16행 / 가드 21행 — 앞선다.

| 항목 | 결과 (worker 실행) |
|---|---|
| 문법 | `bash -n install.sh` PASS |
| **O 정상 경로** | contextMonitor `1 → 0` 제거 · 훅 등록 1 · `▶ 구 Stop 모니터 정리` 출력 |
| **O skip① 앵커 불충족**(`payload=$(cat)`) | contextMonitor `1 → 1` **보존** · 사유 `앵커를 가진 statusline 스크립트를 찾지 못했습니다` |
| **O skip② `statusLine` 없음** | `1 → 1` 보존 · 사유 `.statusLine.command 가 없습니다` |
| **O skip③ statusline 쓰기 실패**(`chflags uchg`) | `1 → 1` 보존 · 사유 출력 |
| **O RED**(제거를 register 단계로 되돌림) | 앵커 불충족인데 `1 → 0` **제거됨** = 결함 재현 |
| **P GREEN** | `CURRENT_USAGE:null` 첫 렌더 → 구 `claude-ctx-{sid}.json`·`-base` **삭제됨** |
| **P RED**(정리를 가드 안쪽으로) | 둘 다 **남음** = 결함 재현 |
| 회귀 | (a)/(b) 차단·대조군 전환+등록 · N 완전일치(GREEN 전환금지 / RED 4건 전환) · 중복 2개 탐지 · 디바운스 5회 · 51,133/59,000 · 퍼미션 `600→600`(RED `600→644`) · 심링크 유지(RED 끊김) · 멱등 1개 · 정규화 유실 0 · 임시파일 0(RED 2개) · 마커 1 · 3-way |
| 실환경 | statusline 은 P 반영으로 **1회 변경**(정상), 재실행 시 `변경 없음`(멱등) · settings/CLAUDE.md md5 무변경 · 퍼미션 644 · 훅 12종 · contextMonitor 0(이미 제거) · `stopEvent.js`·`cleanup-bg-shells.sh` 생존 · 설치 블록==heredoc `diff` 0 · mktemp 잔여 0 · 종단 체인 WARNING/CRITICAL 발화 |

### Codex 13라운드 지적 3건 (Q/R/S) — 반영 완료 · **조언자 검증 전**

착수 확인용: `grep -c 'BACKUP_MAP' install.sh` 가 0 이면 미착수.

**지적 Q [P2] — 두 번째 병합이 원본 백업을 덮어쓴다.** 등록(`register`)과 구 모니터 정리(`cleanup`)가
각각 병합을 돌리는데 백업 이름이 **초 단위**라 같은 초에 끝나면 두 번째가 첫 번째를 덮어썼다.
남는 건 "이미 등록된 상태"의 백업이고 **설치 전 원본이 사라진다.**
→ `BACKUP_MAP`(대상→백업 경로)으로 **실행당 대상별 1회만** 백업한다. 재호출 시 기존 백업 경로를
`LAST_BACKUP` 으로 그대로 넘겨 복원 경로가 끊기지 않게 했다.
카운터 접미사 대신 이 방식을 쓴 이유: 보존해야 하는 것은 **'설치 전 상태' 하나**뿐이다.

**지적 R [P2] — 부분일치로 남의 훅을 덮어쓴다.** 엔트리 탐색이 basename
(`advisor-context-budget.js`) 부분일치라, 다른 프로젝트의 훅이나 같은 이름의 래퍼를 우리 것으로
오인해 커맨드를 조용히 덮어썼다. N 에서 **성공 판정**은 완전일치로 바꿨지만 **탐색 판정**이 느슨하게
남아 있던 계열 잔여다.
→ `CTX_HOOK_KEY="${CTX_HOOK_PATH}"` — 우리가 설치한 **절대경로**로 탐색한다.
(커맨드 전체 문자열로 하지 않는 이유는 node 경로가 바뀌어도 같은 훅을 찾아 **갱신**해야 하기 때문.
 탐색=절대경로 부분일치 / 성공 판정=완전일치, 두 층이 서로 다른 이유가 여기 있다.)

**지적 S [P2] — 배열이 아닌 `hooks.PostToolUse` 를 지워버린다.** 배열이 아니면 **빈 배열로 치환**한 뒤
엔트리를 넣어, 스키마가 다른 설정에서 사용자 데이터가 소리 없이 파괴됐다.
→ 키가 **없을 때만** 생성하고, 있는데 타입이 다르면 **exit 4 로 안전 실패**한다(`hooks` 자체가 dict 가
아닌 경우 동일). 이 실패는 J→N→O 계열대로 **전파**되어 브리지 전환과 contextMonitor 제거가 모두 막힌다.
사유 문자열(`hooks.PostToolUse=str` 등)은 python stderr → 명령 치환으로 받아 안내에 싣는다
(임시파일을 늘리지 않기 위해 변수로 받았다).

| 항목 | 결과 (worker 실행) |
|---|---|
| 문법 | `bash -n install.sh` PASS |
| **Q GREEN** | 설치 1회 후 `settings.json.bak.*` **1개** · 내용이 **설치 전 원본과 md5 동일** |
| **Q RED**(중복 백업 차단 제거) | 백업 1개인데 내용이 **설치 전 원본과 다름**(두 번째가 덮어씀) |
| **R GREEN** | `node /other/project/advisor-context-budget.js --flag` 엔트리 **원문 그대로 보존** · 우리 엔트리 별도 1개 |
| **R RED**(basename 탐색) | 남의 엔트리가 **덮어써짐** |
| **S** (3형태) | `PostToolUse=str` / `PostToolUse=dict` / `hooks=str` → 전부 **md5 무변경 · 브리지 0→0 · contextMonitor 1→1 보존** · `⚠ … 예상 형식이 아닙니다 / 안전을 위해 병합하지 않았습니다` |
| 회귀 | O 4케이스(정상 제거·skip 3형태 보존) · P(가드 앞 정리, RED 남음) · (a)/(b) 차단 · N 완전일치(GREEN 전환금지/RED 4건) · 중복 2개 탐지 · jq 폴백 · 디바운스 5회 · 51,133/59,000 · 퍼미션 `600→600`(RED `600→644`) · 심링크 유지(RED 끊김) · 멱등 1개 · 정규화 유실 0 · 임시파일 0(RED 2개) · 마커 1 · 3-way |
| 실환경 | 재실행 전부 `무변경`(멱등) · **새 `.bak` 증가 0** · 퍼미션 644 · 마커 1 · 훅 12종 · contextMonitor 0 · `stopEvent.js`·`cleanup-bg-shells.sh` 생존 · 우리 엔트리 1개(절대경로 node) · mktemp 잔여 0 |

### Q/R/S 조언자 재현 검증 (2026-07-29) — **통과**

하네스: `/tmp/awb-test/t16_qrs.sh`(수정) · `t17_rgap.sh`(신규) · 결과 원본 `T16_T17_RESULTS.md`.
`install.sh` 는 검증 중 **무수정**(mtime 유지로 확인).

| 과제 | 결과 |
|---|---|
| **하네스 자체 감사** | mutant `assert <대상> in s` 존재 ✓ · 치환 1회 ✓ · 픽스처 소스 = `REAL_settings_BEFORE.json` ✓(실측 `advisor entries: []` · `contextMonitor: 1` 로 설치 전 스냅샷 확인). **결함 1건 발견·수정** — 아래 |
| **Q 결정화** | `date` 스텁(`20260101T000000Z` 고정)으로 두 병합의 백업 파일명 충돌을 **강제**. GREEN: 백업 1개·내용 == 설치 전 원본 / RED(중복 차단 제거): 백업 내용이 **원본과 다름**(원본 유실). 양쪽 다 "두 번째 병합이 실제로 돌았다"(브리지 0→4 · cm 1→0)를 전제 검사 |
| **S exit 4 전파** | `sys.exit(4)` = `:413`(hooks non-dict) · `:423`(PostToolUse non-list). 수신은 `ms_err="$("` (`:387`) … `)" || ms_rc=$?` (`:492`) — **명령 접두사 없는 순수 대입**이라 종료 상태가 명령 치환 상태와 같다(rc=4 캡처 실측). 분기 순서 `-eq 4`(`:539`)가 `-ne 0` 일반 분기(`:554`)보다 **앞** → 뭉개지지 않음. 3형태 전부 md5 무변경·브리지 0→0 |

> **하네스 결함 (5번째 드리프트 함정 · 수정함).** t16 은 `set -u` 만이라 python `assert` 실패 시에도
> 계속 진행했다. mutant 파일이 생성되지 않으면 `bash -n "$QRED" && q_case …` 가 `&&` 에서 단락돼
> **RED 결과 줄이 아예 출력되지 않는데 산문 `echo` 는 그대로 찍혀 "RED 돌았다"로 오독된다.**
> → mutant 생성/`bash -n` 실패 시 `★★ 생성 실패(테스트 무효) ★★` + `exit 1`. 드리프트 시뮬레이션으로 발화 확인.
> **RED 가 통과하면 RED 를 의심하라**는 규칙에, **"RED 가 침묵하면 하네스를 의심하라"**가 추가된다.

### Codex 14라운드 지적 1건 (T) — **미반영**

**지적 T [P2] — jq 폴백이 `null`/`false` 스키마를 강제 변환한다** (`install.sh:499-500`)

```jq
.hooks = (.hooks // {})
| .hooks.PostToolUse = (.hooks.PostToolUse // [])
```

jq 의 `//` 는 좌변이 `false` **또는 `null`** 이면 우변을 쓴다 — "키 없음"과 "명시적 null/false"를
구별하지 못한다. 조언자 직접 재현(L1):

| 입력 | jq 폴백 | python 경로 |
|---|---|---|
| `{"hooks":null}` / `{"hooks":false}` | `{"hooks":{"PostToolUse":[]}}` **치환** | exit 4 안전 실패 |
| `{"hooks":{"PostToolUse":null}}` / `…false}}` | `[]` **치환** | exit 4 안전 실패 |
| `{"hooks":"x"}` | jq 인덱싱 에러(**우연히** 막힘) | exit 4 |
| `{"hooks":{"PostToolUse":"x"}}` | 통과(무변경) | exit 4 |

핵심은 손실 규모가 아니라 **두 경로의 계약 불일치**다. S 가 python 에서 닫은 구멍이 jq 에 남은
계열 잔여이며, 이 문서 26–27행이 스스로 남긴 점검축 ②("예상과 다른 입력을 '고쳐서' 진행하는가")에 걸린다.

**수정 방향(미착수):** jq 표현식 **안에서** `error()` 를 던지면 종료 코드가 python 의 4 와 갈라져
또 다른 계약 불일치가 된다. jq **실행 전** 타입 검사 → python 과 **같은 `schema:` 신호**(`:412·422`)
방출 → 같은 `-eq 4` 분기(`:539`)로 합류시켜야 한다. 문자열 케이스의 현재 방어는 설계가 아니라
우연이므로 함께 명시적 가드로 대체한다.

### 신규 결함 (U) — R 의 반대 케이스 · **미수정**

> 조언자 재현 중 발견. Codex 리뷰 범위 밖이었다(리뷰는 T 만 지적).

R 은 엔트리 **탐색**을 basename → 절대경로(`CTX_HOOK_PATH`)로 좁혀 "남의 훅을 덮어쓰는" 문제를 닫았다.
그 **반대 방향**이 열렸다: **우리 훅을 가리키지만 다르게 표기된 기존 엔트리를 탐색이 놓쳐 이중 등록된다.**

| 변형 | 사전 엔트리 | 결과 |
|---|---|---|
| ③ 심링크 | `"node" "<HOME>/.claude/hlink/advisor-context-budget.js"` | 엔트리 **2개**, **inode 동일**(133701794) · 2회차 `변경 없음` |
| ① 틸데 | `"node" "~/.claude/hooks/advisor-context-budget.js"` | 엔트리 **2개** · 2회차 `변경 없음` |
| ② `$HOME` | `"node" "$HOME/.claude/hooks/advisor-context-budget.js"` | 엔트리 **2개** · 2회차 `변경 없음` |

셋 다 설치기 **exit 0 · `+ PostToolUse 엔트리 추가` · 브리지 4건 전환**으로 **성공 경로를 통과**한다.

원인: 탐색(`key not in str(h.get('command',''))`, `:438`)과 사후조건(`ctx_hook_entry_count`, `:650`)이
**같은 절대경로 key** 로 필터한다 → 다르게 표기된 엔트리는 **탐색에도 계수에도 안 잡혀** `exact:1` 로 통과.
2회차는 자기 엔트리를 찾아 `변경 없음` → **영구·자가침묵**.

**단순 중복이 아니라 N/R 과 같은 계열이다:**
- 확장되면(①②·③) 같은 파일 → 훅이 도구 호출마다 **2번 실행**
- 확장 안 되면(①②) `node` 가 리터럴 `~/…` 를 못 열어 **영구 exit 127 엔트리**가 매 호출 발화
- 갱신 루프(`:443-448`)도 key 매치만 고쳐 이 엔트리는 **영원히 갱신 대상 밖** → node 경로가 바뀌면
  실행 불가 엔트리가 되는데 사후조건은 계속 `exact:1` = **"실행 불가능한 훅이 남는데 성공으로 통과"**

**수정 방향(미착수):** basename 계수로 되돌리면 R 의 GREEN(`/other/project/…` 보존)이 깨진다.
**경로 동치성**으로 판정해야 한다 — 커맨드에서 경로 토큰을 뽑아 `~/`·`$HOME`·`${HOME}` 확장 후
심링크 해석(`realpath`)하여 `CTX_HOOK_PATH` 의 realpath 와 **완전 일치**할 때만 우리 것으로 본다.
그러면 남의 훅은 realpath 가 달라 보존되고, 별칭 표기는 갱신 대상으로 들어온다. python·jq **양쪽**에 필요.

**출처 한정:** 설치기 자신은 틸데 형태를 쓰지 않는다 → 수동 편집·dotfiles 동기화 경유에서만 발생한다.
**미검증 가정:** 틸데/`$HOME` 변형이 런타임에 실제로 2번 실행되는지는 Claude Code 가 훅 커맨드를
셸 경유로 실행하는지에 달렸고 이 리포에서 판정 불가. 설치기 측 사실(탐색 miss → 중복 생성 → `exact:1` 통과)만 주장한다.
심링크 변형은 절대경로라 그 가정이 필요 없다(inode 동일 실측).

### 미결 결정 해소 — `GSD_HOOK_PIN` 제거 **승인** (2026-07-29)

9라운드부터 4회 이월된 건. **승인한다.**
- 그 함수는 GSD 플러그인 소유 `gsd-context-monitor.js` 의 버전을 핀(`1.26.0`)과 대조했다. 번들이
  자기 훅을 소유하게 되어 **판정 대상이 배선에서 빠졌다**
- 남기면 GSD 버전업 시 "브리지를 써도 컨텍스트 경고가 주입되지 않습니다"라는 **거짓 경고**를 낸다 —
  이 작업이 없애려던 R1(플러그인 내부 의존)을 경고 문구로 되살리는 셈
- 기계적 확인: 워킹트리 `GSD_HOOK_PIN`·`check_gsd_hook_version` **0건**, HEAD 에는 정의(32·115행)+호출(620행)
  → **정의와 호출이 함께** 제거되어 dead reference 없음
- Codex 14라운드는 이 제거에 **아무 말도 하지 않았다.** 그것은 승인이 아니라 무언급이다 —
  **조언자 자체 분석으로 승인한 것**이며, 근거는 위 3항(특히 dead reference 기계 확인)이다

---

## 다음 세션 인계 (2026-07-29 시점)

**상태:** 브랜치 `feat/ctx-own-hook` · **전부 미커밋** · 실환경(`~/.claude/`)에는 이미 설치 반영됨.

**바로 할 일**
1. `node ~/.claude/plugins/cache/openai-codex/codex/*/scripts/codex-companion.mjs review --scope working-tree`
   — **13라운드(Q/R/S) 분이 미검증**이다(12라운드 O/P 는 조언자 재현 통과). 장시간이면 `nohup … & disown` 후 `status`/`result` 폴링.
2. 통과 시 커밋. 제안: `feat(bundle): 컨텍스트 예산 훅을 번들 소유로 전환 (GSD 의존 제거)`

**미결 결정 1건 (조언자 판단 필요)**
- `install.sh` 에서 `GSD_HOOK_PIN` + `check_gsd_hook_version()` 을 **제거했다**. 9라운드부터 승인 대기 중.
  근거: GSD 내부 의존이 사라져 버전 대조가 무의미해졌고, 남기면 "브리지를 써도 컨텍스트 경고가
  주입되지 않습니다"라는 **거짓 경고**를 낸다. 되돌리려면 그 함수와 호출 1줄을 복원하면 된다.

**검증 하네스 위치:** `/tmp/awb-test/` (t2·t2b·t3·t5·t8·t11·t12·t13·t14·t15 + `normcmp.py`).
`/tmp` 라 재부팅 시 소멸한다 — 필요하면 먼저 복사할 것.
`REAL_settings_BEFORE.json` 이 **설치 전 스냅샷**이며 모든 픽스처의 소스다(아래 재현 주의 참조).

**설계 계보 요약 (9~12라운드는 전부 한 계열이었다)**
> "쓰기·설치가 실패해도 조용히 성공으로 통과한다"
> G/H/I(대상 파일 취급) → J(등록 실패 시 브리지 전환) → N(쓰기 성공 '판정') → O(대체 수단 확인 전 제거)
> → R(엔트리 '탐색' 판정) → S(스키마 불일치를 치환으로 삼킴) → Q(2회 백업이 원본을 덮어씀).
> 봉합 원칙은 하나다 — **결과를 믿지 말고 다시 읽어 확인하고, 실패는 호출부로 전파한다.**
> 새 기능을 얹을 때 이 원칙을 깨는지부터 볼 것.

> **재현 주의 — 픽스처는 "설치 전" 스냅샷이어야 한다.** 실환경에 이미 설치한 뒤 `~/.claude/settings.json`
> 을 픽스처로 쓰면 병합이 `변경 없음` 으로 빠져 **기준 2(파손 복원)·7-b 가 거짓 실패**한다
> (게이트 주입 지점에 도달하지 못하고, contextMonitor 가 이미 없어 "보존" 단언이 깨진다).
> 설치 전 사본을 떠 두고 그것을 픽스처 소스로 쓸 것. 검증표의 수치는 그 방식으로 재현했다.

### 다음 단계

1. **Codex 검증** — `node ~/.claude/plugins/cache/openai-codex/codex/*/scripts/codex-companion.mjs review --scope working-tree`
2. 통과 시 커밋 (사용자 지시 대기). 제안: `feat(bundle): 컨텍스트 예산 훅을 번들 소유로 전환 (GSD 의존 제거)`

## 알려진 잔여 사항

- ~~**R1(High)**: `gsd-context-monitor.js` 는 GSD 플러그인 소유~~ → **2026-07-29 해소.** 번들이 훅을 직접 소유한다(위 전환 절)
- **신규**: `settings.json` 병합은 파싱→덤프 왕복이라 **원본 바이트 보존이 원리적으로 불가능**하다(들여쓰기·키 순서는 유지되나 파일이 재직렬화된다). 보존 검증은 정규화 JSON 깊은 비교로 한다
- **신규**: 유효성 게이트②는 정상 경로에서 도달하지 않는다(파싱→덤프는 항상 유효한 JSON). 잘린 쓰기·중단된 쓰기 대비 안전망이며 픽스처로만 발동을 확인했다
- **신규**: `external-memory/SKILL.md` 는 34행 트리거 항목만 고쳤다. **86·110·132행에 "150K" 표현이 남아 있다**(브리프 범위 밖) — 후속 정리 필요
- **신규**: **죽은 세션**의 구 배선 파일(`claude-ctx-{sid}*`)은 그 세션의 statusline 이 다시 렌더되지 않으므로 남는다. GSD 훅의 60초 staleness 가드가 걸러 무해하며, 정리하려면 수동 삭제가 필요하다
- **신규**: `install.sh` 에서 `GSD_HOOK_PIN` / `check_gsd_hook_version()` 을 **제거했다**(판단 필요 — 조언자·Codex 승인 대상). 근거: GSD 내부 구현 의존이 사라져 버전 대조가 무의미해졌고, 남겨두면 "브리지를 써도 컨텍스트 경고가 주입되지 않습니다" 라는 **거짓 경고**를 낸다. 되살리기를 원하면 되돌릴 수 있다
- **갱신(해소)**: 결함 U 잔여였던 `nested`(`sh -c "node …"`) · `badq`(따옴표 불균형) 2형태는 **토큰 '추출'만 넓혀** 닫았다 — 각 토큰을 whitespace 로 재분해하고, 확장 전에 앞뒤 `"`·`'` 를 strip 한다. **'판정'(realpath 완전일치)은 그대로**라 R 보호는 손대지 않았다(실측: `node /other/project/advisor-context-budget.js --flag` 원문 보존). 두 형태 모두 엔트리 1개 수렴·2회차 `변경 없음`. `jq` 경로는 판정이 `contains`(부분일치)라 애초에 토큰화하지 않으므로 **이 2형태를 이미 덮고 있었다**(수정 불필요 — 실측 확인)
- **신규(잔여)**: **토큰 추출은 1단계 재분해까지만 덮는다.** 더 깊은 셸 중첩·`eval` 체인처럼 경로가 whitespace 로 분리된 낱말로 드러나지 않는 표기는 여전히 python3 경로에서 놓친다(의도적 — 임의의 셸 문법을 쫓지 않는다). **`jq` 경로는 심링크 별칭을 원리적으로 놓친다** — jq 에 `realpath` 가 없어 표기 4형태(절대경로·`~/…`·`$HOME/…`·`${HOME}/…`) 문자열 동치만 덮는다(실측: jq 심링크 변형 엔트리 2개). 놓치면 탐색·계수가 함께 놓쳐 **이중 등록이 `exact:1` 로 조용히 통과한다.** 출처는 U 와 같다 — 설치기 자신은 이런 표기를 쓰지 않으므로 수동 편집·dotfiles 동기화 경유에서만 발생한다
- **신규(해소)**: **'별칭 + 절대경로 공존' → `dup:2` 설치 중단**은 dedupe 로 닫았다. **판정을 통과한 엔트리들 사이에서만** 첫 번째를 제자리에 남기고 커맨드를 정규값으로 갱신, 나머지는 제거한다(python·jq 양쪽). 판정에 실패한 엔트리는 세지도 지우지도 않는다. 자리를 옮기지 않는 이유는 jq 경로가 `cmp -s` 로 멱등을 판정하기 때문. `deduped` 를 '변경 없음(exit 10)' 조건에 **반드시** 포함해야 한다 — 빠뜨리면 `[정규, 정규]`(중간 빌드를 이미 돌린 실제 모집단)에서 `added`·`updated` 가 모두 거짓이라 파일이 그대로 남아 `dup:2` 가 유지된다
- **신규(잔여)**: **`jq` 의 `contains` 는 접미사가 붙은 남의 경로도 우리 것으로 오인한다**(`…advisor-context-budget.js.disabled`). 이 과잉매칭 자체는 **기존 결함**이며 이번에 고치지 않았다 — 그런 엔트리가 혼자 있으면 커맨드가 정규값으로 **덮어써진다**(수정 전과 동일, 실측 대조 완료). 다만 dedupe 가 그 느슨함을 물려받아 **삭제**로 번지지 않도록, 지울 때만 토큰 완전일치(`cmd_is_exact_ours`)를 요구한다. 귀결: 과잉매칭 엔트리가 정규 엔트리와 공존하면 계수기는 여전히 `dup:2` 를 보고하고 **브리지가 전환되지 않는다**(조용한 오성공이 아니라 보이는 실패). python3 경로는 `realpath` 완전일치라 이 과잉매칭이 없다
- **신규(잔여)**: `cleanup` 모드(`do_add=0`)는 dedupe 하지 않는다 — 엔트리를 건드리지 않는 것이 그 모드의 계약이라 2개 남은 머신은 cleanup 경로에서 여전히 검증에 걸린다(기존 동작, 범위 밖)
- **신규(함정 · jq 블록 편집자용)**: 병합 jq 프로그램은 **bash 홑따옴표 문자열**이다. 주석에라도 홑따옴표를 쓰면(`'첫 번째만'` 등) 문자열이 닫혔다 열려 **인자가 조용히 쪼개진다**. `bash -n` 은 따옴표 짝이 맞아 **통과하고**, 설치기는 `jq` 실패를 `2>/dev/null` 로 삼켜 `settings.json 을 파싱할 수 없습니다`(rc=3)로만 보인다 — 원인과 증상이 완전히 어긋난다. jq 쪽 새 코드의 홑따옴표는 전부 `'` 로 쓴다(`ctx_toks` 의 `gsub` 참조). 실제로 이번에 한 번 밟았고 **하네스 양성 대조가 잡아냈다**
- **신규(측정 함정)**: **브리지 전환 여부를 statusline 마커 유무로 판정하면 안 된다.** 테스트 픽스처가 실환경 `awesome-statusline.sh` 를 복사해 오면 **이미 마커가 들어 있어 항상 참**이 된다(거짓 양성 — RED 가 침묵한다). 로그의 `브리지를 전환하지 않았습니다` 유무로 판정하라. `t18_u.sh` 의 `bridged()` 가 이 방식이다
- **신규**: `jq` 폴백 병합 경로는 `python3` 부재 환경 전용이다. `python3` 만 제거한 PATH 로 검증했고 python3 경로와 **동일한 결과**(엔트리 1개·내부 훅 단위 제거·멱등 `diff` 0)를 냈다. 다만 실사용 경로는 아니므로 회귀 위험은 python3 경로보다 크다
- `isGsdActive` 판정은 신뢰 불가 — `.planning/STATE.md` 가 실재해도 비-GSD 분기가 떴다(`data.cwd` 가 프로젝트 루트가 아님). REVIEW §1.3 정정 블록 참조
- 서브에이전트 `tools:` 필드의 MCP 와일드카드 지원 여부 **미검증** — `researcher` 는 `WebSearch, WebFetch, Read, Grep, Glob` 만 부여
- `~/.claude/hooks/universal/contextMonitor.js` 폐기 검토 = 별도 후속 과제(범위 밖)
- **업그레이드 시 진행 중 세션은 예산이 1회 리셋된다** — 기존 세션에는 `-base` 만 있고 `-prev` 가
  없어 재설치 후 첫 렌더가 '최초 관측' 경로를 타고 `base := 현재값`(이미 커진 값)으로 재래치된다.
  prev 도입의 직접적 귀결이며 다음 세션부터는 정상. 디바운스 상태는 함께 지워지므로 경고 유실은 없다
- `^input=` 앵커는 **존재만** 확인하고 위치는 보지 않는다. `CURRENT_USAGE=` 가 `input=` 보다 앞선
  스크립트라면 블록이 `input` 대입 전에 놓일 수 있다(현실적으로 `CURRENT_USAGE` 는 `$input` 에서
  파생되므로 발생하지 않는다). 순서 검사는 미구현
- 실환경 `~/.claude/awesome-statusline.sh` 는 일반 파일이라 심링크 경로는 **가짜 HOME 픽스처로만**
  검증됐다(실사용 경로는 미경유)
- **구 블록 정리의 사각지대 2건(의도적 범위 밖)**: ① `jq` 미설치 환경은 함수 최상단에서 return 하므로
  정리도 못 한다(브리지 본문 자체가 jq 를 쓰므로 그 환경에선 애초에 동작 불가) ② `^CURRENT_USAGE=`
  까지 개명해 **후보로도 안 잡히는** 스크립트는 손댈 파일을 특정할 수 없다. 둘 다 남은 블록은
  수동 제거가 필요하다
- 마커가 **2쌍 이상**이거나 `start`/`end` 짝이 안 맞는 파일은 삽입·정리 **양쪽 다 무동작**(경고만).
  자동 판단이 사용자 코드를 지울 위험이 있어 "수동 정리 후 재실행" 을 안내한다 — 확인된 동작이다
- 백업 파일명 타임스탬프가 **초 단위**(`date -u +%Y%m%dT%H%M%SZ`)라 같은 초에 두 번 설치하면
  뒤 백업이 앞 백업을 덮어쓴다. 기존 동작이며 실사용(연속 재실행)에서는 문제되지 않으나,
  테스트에서 백업 개수를 세면 1개로 보일 수 있다
- **신규(V 잔여 · 의도적 수용)**: 심링크 거부(node `O_NOFOLLOW` · bash `mktemp`+`mv`)는 **무결성**(덮어쓰기·내용 주입)만 닫는다. 남은 것 4건 —
  ① **정보 노출**: 공유 `/tmp` 에서 다른 로컬 사용자가 `claude-ctx-advisor-{sid}*` 를 **읽어 토큰 사용량을 관찰**하는 것은 막지 못한다. 파일 모드는 600 으로 좁혔으나 **파일명 자체가 session_id 를 드러낸다.** 경로 이전 없이는 닫히지 않고, 경로를 옮기면 하네스·브리지 계약(t16 재래치·t18/t19 브리지 단언·디바운스 테스트)이 전부 무효화되므로 수용했다
  ② **가용성(DoS) — 미해결**: 공격자 소유 심링크가 `-base` 나 `.json` 에 놓이면 **경고가 영구히 꺼진다.** sticky `/tmp` 에서는 자기 소유가 아닌 엔트리를 교체할 수 없어 `rename(2)` 이 **EPERM** 을 낸다(POSIX 로부터의 추론 — 재현에 **다른 UID 가 필요해 미검증**. 같은 사용자·`chmod 1777` 로는 그냥 성공한다). 특히 `-base` 는 **재래치 때만** 다시 쓰이므로, 공격자가 큰 수치를 심어두면 `relatch=0` 이 되어 심링크가 교체될 기회조차 오지 않고 `delta` 가 영원히 음수가 된다. 심링크→**디렉토리** 변형은 실측했다(`mv` rc=0 · 심링크 유지 · 파일이 디렉토리 **안으로** 이동). 수정 전 `>` 도 같은 자리에서 실패하던 **기존 동작**이라 범위 밖
  ③ **bash 쪽 읽기는 여전히 심링크를 따라간다** (`__ctx_base=$(cat "$__ctx_base_f")` · `-prev` 동일). ②의 지속성을 만드는 기전이 이것이다. 닫으려면 `[ -L ]` 사전검사가 필요한데 TOCTOU 가 남아 완전하지 않다 — 범위 밖 (node 쪽 읽기 2곳은 `O_NOFOLLOW` 로 닫았다)
  ④ **임시파일 누수**: 렌더가 `mktemp` 와 `mv` 사이에서 죽으면 `claude-ctx-advisor-wtmp.*` 가 1개 남는다. statusline 은 매 턴 돌므로 누적될 수 있고, 위 `rm -f` 정리 목록은 이 이름을 매칭하지 않는다. 구 `>` 방식은 남기지 않았다 — 원자성과 맞바꾼 대가
- **`REVIEW_…md` §4 앵커 표가 8라운드 기준으로 스테일** — `^input=` 로 적혀 있고 `export`·선행 공백
  허용이 반영되지 않았다. 7라운드의 "수동 수리본은 재설치에서 보존된다" 안내도 §4 에 없다.
  설치기 경고가 사용자를 §4 로 보내므로 갱신이 필요하다(라운드 범위가 install.sh+HANDOFF 라 미수정)

---

## 다음 세션 인계 (2026-07-29 · 2차) — **완료 · 이력**

> **U·T 는 2026-07-29~30 에 모두 수정됐다.** 최신 상태는 문서 맨 아래 **`## 다음 세션 인계 (2026-07-30 · 3차)`** 를 보라.
> 이 절은 당시의 판단 근거(우선순위 논거·실환경 점검)를 남기기 위해 보존한다.

**상태(당시):** 브랜치 `feat/ctx-own-hook` · 워킹트리 미커밋 · 실환경(`~/.claude/`)에는 이미 설치 반영됨.
**Q·S = 조언자 재현 통과. R = 작성된 케이스에는 유효하나 그 좁힘이 U 를 열었다.**
Codex 14라운드의 Q/R/S 무지적은 통과 증명이 아니다("못 찾음") — U 가 그 증거다.

**당시의 할 일 — 우선순위 순**

1. **결함 U 수정 [우선]** (별칭 표기 엔트리 이중 등록). 탐색·계수를 **경로 동치성**(틸데/`$HOME`/
   `${HOME}` 확장 + `realpath`)으로 바꾼다. python·jq 양쪽.
   **R 의 GREEN(`/other/project/…` 보존) 무회귀가 완료 기준.**
2. **지적 T 수정** (jq 폴백 null/false 강제 변환). jq 실행 **전** 타입 검사 → python 과 같은
   `schema:` 신호 → 같은 `-eq 4` 분기 합류. 문자열 케이스의 우연한 방어도 명시 가드로 대체.
3. Codex 재검증 → 통과 시 커밋. 제안:
   `feat(bundle): 컨텍스트 예산 훅을 번들 소유로 전환 (GSD 의존 제거)`

**왜 U 가 먼저인가 (하나만 고칠 수 있다면 U):**

| | T | U |
|---|---|---|
| 발동 경로 | `python3` **부재** 환경 전용 (문서화된 저노출) | **실사용 python 경로** |
| 재실행 | — | **자가침묵** (2회차 `변경 없음` → 영구 고착) |
| 결과물 | 병합 skip | 살아있는 `settings.json` 에 **영원히 갱신 불가한 엔트리** |

> **실환경 U 조건 점검 (2026-07-29 · 조언자 실측):** `~/.claude/settings.json` 의 advisor 엔트리
> **1개 · 정규 절대경로 · node 절대경로** = **CLEAN.** 따라서 U 는 현재 진행 중인 장애가 아니라
> **잠재 결함**이며, 다음 세션의 작업은 **코드 수정만**이고 실환경 리메디에이션은 불필요하다.
> (U 는 설치기가 만들지 않는다 — 수동 편집·dotfiles 동기화로 별칭 엔트리가 생긴 환경에서만 발동)

**T 와 U 를 한 번에 고쳐야 하는 이유:** 둘 다 **python 경로와 jq 경로가 다른 계약을 갖는다**는
같은 뿌리다. 한쪽만 고치면 나머지가 다음 라운드에 같은 모습으로 다시 나온다 — J→N→O→R→S 가
정확히 그렇게 반복됐다. **두 경로를 같은 판정 함수에 태우는 것**이 계열을 닫는 방법이다.

**검증 하네스:** `/tmp/awb-test/` — `t16_qrs.sh`(Q/S·감사 가드 추가됨) · `t17_rgap.sh`(U 재현 전용) ·
`T16_T17_RESULTS.md`(원본 결과) · `REAL_settings_BEFORE.json`(**설치 전 스냅샷 · 모든 픽스처의 소스**).
**→ 2026-07-29 `~/.claude/awb-test-harness/` 로 복사해 보존함(18개, `codex13.log` 포함).**
`/tmp` 사본은 재부팅 시 소멸하므로 **보존본을 쓸 것.**

**하네스 규율 3종 (이 세션에서 실제로 물렸다)**
- mutant 는 `assert <대상> in s` + 치환 1회로 못 박는다 — 없으면 RED 가 **침묵**하고 산문만 찍혀 오독된다
- 시간 의존 검증(백업 파일명 등)은 `date` 스텁으로 **결정화**한다 — 안 하면 "재현 안 됨"이 무의미
- 파괴적 재현은 **가짜 HOME 에서만**. 실환경은 무변경·멱등 확인 전용(실 `.bak` 증가 0 유지)

---

## U·T 수정 (2026-07-29~30) — 반영 완료

착수 확인용: `grep -c 'CTX_PY_PATHMATCH' install.sh` 가 0 이면 미착수.
원본 결과: `~/.claude/awb-test-harness/UT_FIX_RESULTS.md`(1차) · `UT_FIX_RESULTS2.md`(잔여 2건).

### U — 경로 동치 판정 (탐색·계수 단일 소스)

`CTX_PY_PATHMATCH` 신설: `shlex.split` 토큰화 → 각 토큰 **재분해**(whitespace) → 앞뒤 `"`/`'` strip →
`~`/`$HOME`/`${HOME}` 확장 → `realpath` → `CTX_HOOK_PATH` 의 realpath 와 **완전 일치**.
**탐색(PYMERGE)과 계수(`ctx_hook_entry_count`)가 같은 변수를 주입받는다** — 두 곳에 복사하면
또 갈라진다(R·N 이 정확히 그 실패였다). jq 는 `CTX_JQ_MATCH` + 4형태 `--argjson keys`.

> **판정은 완전일치, 확장한 것은 토큰 *추출*뿐이다.** R 보호(`/other/project/…` 보존)는
> realpath 완전일치에 있지 토큰을 인색하게 뽑는 데 있지 않다 — 그래서 추출 확대는 공짜다.

### 파생 잔여 2건 — 같은 세션에서 닫음

- **① nested/`badq` 토큰 miss**: `sh -c "node $HOME/…"`(경로가 독립 토큰이 아님) · 따옴표 불균형
  (`shlex` `ValueError` → `.split()` 폴백에 `"` 잔류). → 재분해 + 따옴표 strip 2건으로 포착.
  **1단계 재분해에서 멈췄다**(임의 중첩·`eval` 은 쫓지 않음)
- **② 별칭+정규 공존 → `dup:2` 로 설치 중단**: 수정본이 둘 다 우리 것으로 인식해 둘 다 갱신 →
  동일 커맨드 2개 → 검증 실패 → **브리지 전환 중단**. U 가 보호하려는 모집단이 정확히
  "별칭 엔트리가 있는 머신"인데 **고치려던 환경을 고치지 못하는 막다른 업그레이드 경로**가 된다.
  → **dedupe 구현**: realpath 판정을 **통과한** 엔트리들 사이에서만 1개로 접고 갱신.
  판정 실패 엔트리는 무접촉(R 보호는 계수가 아니라 판정에 있다)

> **`deduped` 를 `exit 10`(변경 없음) 조건에 포함시킨 것이 핵심이다.** `[정규, 정규]` 는
> `added`·`updated` 가 모두 거짓이라 빠뜨리면 exit 10 → 임시본 폐기 → **여전히 `dup:2`**.
> RED 가 그 마스킹을 그대로 재현했다.

### T — jq 스키마 가드 (python 과 동일 계약)

jq **실행 전** `has("hooks")` / `.hooks|has("PostToolUse")` 기반 타입 검사.
`| type` 만으로는 "키 없음"과 "명시적 null" 을 구별할 수 없다 — **이 결함의 원인이 정확히 그것**이다.
jq 안에서 `error()` 를 던지지 **않고** `ms_err="  schema: …"` + `ms_rc=4` 로 **기존 `-eq 4` 분기에 합류**
(던지면 종료 코드가 python 의 4 와 갈라져 또 다른 계약 불일치가 된다).
타입명은 python `type().__name__` 으로 매핑(`str`/`NoneType`/`bool`/`dict`/`list`).

### 검증

| 항목 | 결과 |
|---|---|
| 문법 | `bash -n install.sh` PASS |
| **U GREEN** | 6형태(tilde·`$HOME`·`${HOME}`·심링크·**nested·badq**) 전부 **엔트리 1개** · `~ 커맨드 갱신`(추가 아님) · 2회차 `변경 없음` |
| **U 무회귀 (R GREEN)** | `node /other/project/advisor-context-budget.js --flag` **원문 그대로 보존**(py·jq) |
| **U RED** | 판정 되돌림 → 4형태 2개 / 재분해·strip 제거 → nested·badq 2개 복귀(대조군 tilde 는 1개 유지) |
| **dedupe GREEN** | `[별칭,정규]`·`[심링크,정규]`·`[정규,정규]` 3형태 **정확히 1개**(정규 커맨드) · **브리지 전환됨** · 2회차 `변경 없음` · 남의 훅 공존 시 **보존** |
| **dedupe RED** | 제거 시 **2개 · `dup:2` · 브리지 ★미전환★** 복귀 (py·jq 각각 전용 mutant) |
| **T GREEN 6형태** | `hooks` null/false/str · `PostToolUse` null/false/str → **py·jq 완전 동일**(md5 무변경 · 브리지 0→0 · 경고 문구까지) |
| **T RED** | 가드 제거 시 `null`→`{"PostToolUse":[…]}` · `false`→`[…]` **치환 재현** |
| **S 전파 재확인** | PYMERGE 를 `{ printf; cat <<'PYMERGE'; } \| python3 -` 파이프로 바꾼 뒤에도 `set -euo pipefail`(`:16`) 하에서 **`ms_rc=4` 전파**(조언자 마이크로 재현 `rc=4` + 실경로 3형태 경고 문구 출력) |
| 기존 회귀 | t16(Q/R/S) · t8 · t9 · t11(퍼미션 `600→600`·심링크·Orca 40 유실 0) · t13(임시파일 0) **전부 통과** |
| 실환경 | settings.json md5 **세션 전후 동일** · advisor 엔트리 1개 · 오늘 mtime 백업 **0개** |

### 방법론 산출물 — 기존기준 대조 (baseline differential)

**"내가 만든 손실"과 "원래 있던 손실"은 화면에서 똑같이 `★소실★` 로 보인다.**
가르는 방법은 하나뿐이었다 — **문제의 수정만 제거한 빌드를 같은 픽스처에 돌려 나란히 놓는 것.**

이 대조가 실제로 회귀 1건을 잡았다: jq 의 `cmd_is_ours` 가 `contains` 라 `…advisor-context-budget.js.disabled`
같은 **접미사 경로도 우리 것으로 오인**하는데, 초기 dedupe 가 그 느슨함을 물려받아 **삭제로 번졌다**.

| 픽스처 | 초기 구현 | 기존기준 | 판정 |
|---|---|---|---|
| `.js.disabled` 단독 | ★소실★ | ★소실★ | 동일 → **기존 결함**(덮어쓰기), 내가 만든 게 아니다 |
| `.js.disabled` + 정규 | ★소실★ | 보존 2개 | **갈라진다 = 내가 만든 회귀(삭제)** |

→ `cmd_is_exact_ours`(토큰 완전일치)를 **삭제 결정에만** 적용해 닫았고, 재측정에서 6칸 모두 기존기준과 일치.

### 하네스 함정 (이번에 새로 물린 것)

- **`t16` 의 R RED 가 무효화됐다** — U 수정으로 `CTX_HOOK_KEY` 를 부분문자열로 쓰지 않게 되면서,
  basename 으로 바꿔도 "basename 매칭"이 아니라 **"아무것도 매칭 안 함"**(우리 엔트리 0개)이 된다.
  → t18 에 **판정 자체를 basename 비교로 바꾸는** R RED 를 신설. mutant 드리프트의 6번째 형태다
- **`bridged()` 가 거짓 양성이었다** — statusline 마커로 판정했는데 픽스처가 복사하는 실환경
  `awesome-statusline.sh` 에 **이미 마커가 있어** 항상 참. → 로그의 `브리지를 전환하지 않았습니다`
  유무로 바꾸자 그제서야 RED 가 `★미전환★` 을 보고했다
- **jq 프로그램은 bash 홑따옴표 문자열**이라 주석에 `'` 를 쓰면 인자가 조용히 쪼개진다.
  `bash -n` 은 통과한다(따옴표 짝이 맞아서) — 양성 대조가 잡았다

---

## 다음 세션 인계 (2026-07-30 · 3차)

**상태:** 브랜치 `feat/ctx-own-hook` · 워킹트리 **미커밋** · 실환경 설치 반영됨(무변경 확인).
**Q·R·S·T·U + 파생 잔여 2건 전부 반영·검증 완료.**

**바로 할 일**
1. **Codex 15라운드 결과 확인** — `/tmp/awb-test/codex15.log` (2026-07-30 발사, `--scope working-tree`).
   프로세스가 죽었으면 재실행: `nohup node ~/.claude/plugins/cache/openai-codex/codex/*/scripts/codex-companion.mjs review --scope working-tree > /tmp/awb-test/codex15.log 2>&1 & disown`
2. 지적이 나오면 반영 → 재검증. 없으면 **커밋(사용자 지시 대기)**. 제안:
   `feat(bundle): 컨텍스트 예산 훅을 번들 소유로 전환 (GSD 의존 제거)`

**검증 하네스:** `~/.claude/awb-test-harness/` (보존본) — `t18_u.sh`(U·dedupe) · `t19_t.sh`(T) ·
`t16_qrs.sh`(Q/R/S) · 프로브 4종(`pymatch_probe`·`predprobe`·`jqprobe`·`bprobe`) · 기존 회귀 t8/t9/t11/t13.
재현 명령은 `UT_FIX_RESULTS2.md` §8.

**하네스 규율 (누적 5종)**
- mutant 는 `assert <대상> in s` + 치환 1회 + 생성 실패 시 `exit 1`
- 시간 의존 검증은 `date` 스텁으로 결정화
- 파괴적 재현은 가짜 HOME 에서만
- **양성 대조를 선두에 둔다** — 주입 코드의 문법 오류로 계수기가 조용히 전멸하는 것을 "결과"로 오독하지 않기 위해
- **판정 헬퍼를 바꾸면 그 헬퍼를 겨냥한 기존 mutant 가 무효화됐는지 확인한다**(t16 R RED 사례)
- **RED 는 '결함이 있는 쪽'을 되돌려야 한다**(W 사례 · 아래)

---

## Codex 16라운드 지적 2건 (W/X) — 반영 완료 (2026-07-30)

결과 상세·재현 명령: `~/.claude/awb-test-harness/WX_FIX_RESULTS.md`
신규 하네스: `t21_x.sh`(PASS 20) · `t22_w.sh`(PASS 10). 기존 회귀 12종 전부 통과(`/tmp/awb-regress.log`).

### X — cleanup 검증 실패가 '방금 등록한 훅'을 지웠다 (P2)

`ms_backup`(=`LAST_BACKUP`/`BACKUP_MAP`)은 **Q 수정으로 대상당 1개 = register 이전**(훅 미등록)이다.
그런데 `cleanup` 은 register 성공 **및 브리지 전환 이후에만** 돈다 → 검증 실패 시 그 백업으로
복원하면 **훅 등록이 사라지고 브리지는 이미 advisor 메트릭으로 전환된 뒤**라 구 모니터도 새 훅도
발화하지 못한다. **경고 전면 사망**이 조용히 성립한다.

→ **cleanup 전용 '단계 직전 스냅샷'** 으로 복원(`install.sh:761-782`). 되돌릴 올바른 상태는
cleanup 자신의 직전(등록됨 + 전환됨 + 구 모니터 잔존)이다. "복원하지 않기"는 배제 — 쓰기가 파일을
잘라먹으면 깨진 `settings.json` 이 남고, 그것이 stale 모니터 잔존보다 나쁘다(잔존은 무해: 브리지가
전환돼 GSD 훅은 `claude-ctx-{sid}.json` 을 못 찾고 종료).

**함정:** 스냅샷을 `.bak.<ts>` 로 만들면 **Q 하네스의 "`.bak` 정확히 1개 + 내용 = 설치 전 원본"이
깨져 X 를 닫으며 Q 가 다시 열린다.** → 같은 디렉토리 내부 `mktemp` 점파일(`.settings.presnap.*`),
`BACKUP_MAP`/`LAST_BACKUP` 과 변수·이름 분리, 4개 이탈 경로 전부에서 제거.
스냅샷 생성 실패는 **쓰기 전에 `return 1`** — `ms_backup` 폴백은 구조상 확정적으로 틀리므로 금지.
cleanup 실패는 호출부(`:1430`)가 로그만 하고 계속하므로 무해.

RED(수정 전) advisor 엔트리 **0개** / GREEN **1개 생존** + 구 모니터 잔존. Q 무회귀 동시 단언.

### W — 훅과 브리지의 임시 디렉토리 해석이 갈렸다 (P2)

브리지(bash) `${TMPDIR:-/tmp}` 는 `TMP`·`TEMP` 를 무시하고, 훅(node) `os.tmpdir()` 은 본다.
`TMP` 만 설정된 환경에서 **브리지는 `/tmp` 에 쓰고 훅은 `$TMP` 를 읽어** 메트릭이 영영 만나지 않는다
→ 모든 컨텍스트 예산 경고가 조용히 억제되고, 재래치의 `-warned.json` 삭제도 빗나가 디바운스가 승계된다.

→ 양쪽을 `TMPDIR → TMP → TEMP → /tmp` 로 정렬. bash `:1127`, node `:302-303`(후행 슬래시를
bash 의 `%/` 와 똑같이 **한 개만** 제거 · 빈 문자열은 `/`). 미사용이 된 `require('os')` 제거.
`$TMPDIR` 표기가 바뀐 순서를 잘못 설명하게 된 주석 3곳 갱신.

**실측 고지 — node 변경은 POSIX 에서 무동작이다.** `os.tmpdir()` 이 이미 같은 순서라
5개 env 조합 전부에서 경로가 동일했다. **실제 수정은 bash 한 줄**이고, node 편집의 값은 계약 명시
(win32 는 `TEMP` 우선)와 슬래시 규칙 고정뿐이다.

**하네스 함정(신규 · 7번째 형태):** RED 로 **node 를 되돌리면 양쪽이 여전히 `$TMP` 로 수렴해
RED 가 통과한다** — 공허한 테스트. RED 는 **결함이 있는 쪽(bash)** 을 되돌려야 한다.
W-① RED 는 정의상 실 `/tmp` 로 쓴다 → 세션 고유 SID + 쓰였음을 양성 증거로 확인 후 삭제·잔여 0 단언.

### 실환경

**설치기를 실행하지 않았다.** settings.json·statusline·훅 md5 전부 무변경, 새 `.bak` 0,
`.settings.presnap.*` 0, advisor 엔트리 1. 적용은 Codex 승인 후 별도 단계
(지금 적용하면 statusline·훅이 갱신되어 "무변경" 기준을 스스로 깬다).
